# Claude Code Project Memory

> **Full version history lives in `CHANGELOG.md`.** This file is project memory: workflow, conventions, current state. Only the most recent change stays here as a pointer.

## Git Workflow
- Always push changes directly to `main` branch
- Commit with clear, descriptive messages
- Push after every commit — do not batch pushes
- **Bump version number with EVERY change** (patch: 0.5.x). **Seven files MUST stay in sync:**
  1. `CLAUDE.md` → `## Current Version` line
  2. `VERSION` (just the number, e.g. `0.5.74`)
  3. `sw.js` → `CACHE_VERSION = 'coach4u-vX.Y.Z'`
  4. `business.html` → visible label at the bottom of the dashboard footer (`<p ...>vX.Y.Z</p>`)
  5. `index.html` → visible label at the bottom of the account dashboard footer (added v0.5.143)
  6. `account-users.html` → visible label at the bottom (added v0.5.158 after the footer was found stuck on v0.5.153)
  7. `account-setup.html` → visible label at the bottom (added v0.5.158 — created in v0.5.153, never bumped till now)
- Append a new entry to `CHANGELOG.md` for every bump. Keep the most recent 1–2 entries duplicated under `## Latest` in this file as a pointer.
- For large file changes: split into small focused files (each under ~8KB) to avoid push timeouts

## Project Overview
- **Your Business Coach** portal — PWA with Supabase authentication
- Hosted on GitHub Pages
- Uses email + password sign-in via Supabase
- **Repo visibility**: public (this is a GitHub Pages site). The Supabase anon key in this file is the `sb_publishable_*` (anon) variant, which is intentionally publishable — it's not a server-side secret. All security relies on Supabase RLS policies. **Never commit a `service_role` key.**

## Wording / Tone
- **No EOS jargon.** Removed in v0.5.116. Don't reintroduce: "Level 10" / "L10", "V/TO", "Vision/Traction Organizer", "Same Page Meeting", "IDS" / "Identify, Discuss, Solve" as a label, "Accountability Chart", "Right seat" / "right people", "GWC", "Quarterly Projects", "10-Year Target", "3-Year Picture", "Annual Meeting", "Headlines" (as a meeting agenda item), "Proven Process", "three uniques", verbatim "EOS", "Rocks" in user-facing copy. Use generic equivalents: "Weekly Team Meeting", "Org Chart", "Quarterly Goals", "10-Year Vision", "3-Year Outlook", "Annual Planning Session", "Customer & Team Highlights", "Discuss & Resolve", "Our Process", "What makes us different".
- The DB table `rocks` keeps its name (renaming would require a migration). UI surfaces it as "Quarterly Goals".

## Design System

**One design.** Aptos system stack, navy `#003366` primary, teal `#0D9488` accent — applied to every page in the project.

- Primary (navy): `#003366` | Primary dark: `#002244` (hover)
- Accent (teal): `#0D9488` | Accent dark: `#0F766E` (hover)
- Text: `#333333` | Muted: `#888888`
- Font: **Aptos system stack** — no Google Fonts
- Card border: `2px solid var(--accent)` or `1px solid var(--border)` depending on prominence
- Border-radius: `10px`
- Tip box (`.ws-hint`): `#f8fafc` bg, teal left border — used for "Explore X exercises in the Learning Vault →" prompts

Stylesheets:
- `css/style.css` — main design system v2.2 (loaded on every page)

## File Structure
```
index.html              — root portal: account dashboard with multi-tenant switcher
business.html           — business-level dashboard (one biz)
setup.html              — first-business wizard for brand-new users

# Hubs (business-level)
strategy.html           — Strategy hub (cards link to root worksheets) + View One-Page Plan
operations.html         — Operations hub + Run Weekly Meeting + View One-Page Operations
planning.html           — Planning hub (annual + quarterly session lists)
learning-vault.html     — reference / how-to area

# Strategy worksheets (Supabase-backed, scoped by organisation_id)
core-values.html, core-focus.html, targets.html,
marketing-strategy.html, leadership-team.html, financials.html
twelve-month-goal.html  — single-textarea editor for targets.one_year_goals (Strategy hub card "12 Month Goal")

# Operations tools (Supabase-backed, scoped by organisation_id)
scorecard.html          — Weekly Numbers
goals.html              — Quarterly Goals (rocks table)
meeting.html            — past weekly meetings (list)
run-meeting.html        — single weekly meeting workspace
issues.html             — open + resolved issues (?category=current|future|yearly)
todos.html              — cross-meeting open to-dos (v0.5.204)

# Planning (Supabase-backed)
cadence.html            — Planning Cadence (business_cadence — annual + quarterly dates, weekly day+time) v0.5.192
annual-sessions.html, run-annual-session.html
quarterly-sessions.html, run-quarterly-session.html
team-checkin.html       — auth-gated team-facing check-in form (17 rated statements + name + optional role)

# Printable one-pagers (read-only)
one-page-plan.html         — landscape A4 strategy doc (per business)
one-page-operations.html   — landscape A4 ops doc: Quarterly Goals + Weekly Numbers + Open Issues (per business)

# Account-level cross-business pages
account-strategy.html, account-operations.html, account-planning.html  — account-level hubs
account-plans.html              — carousel of every business's one-page strategy doc
account-ops-plans.html          — carousel of every business's one-page operations doc
account-values.html, account-focus.html, account-targets.html,
account-marketing.html, account-leadership.html                — per-worksheet carousels
account-numbers.html, account-goals.html, account-meetings.html, account-issues.html
                                                              — per-operations-tool carousels
account-annual.html, account-quarterly.html, account-checkins.html
                                                              — planning carousels

# Auth + utility
login.html, forgot-password.html, reset-password.html
inactive.html           — shown when membership_status ≠ 'active'
offline.html, 404.html

learn/
└── values-discovery.html       — guided 3-step values exercise (localStorage)

css/
└── style.css           — Design 1 system v2.2

js/
├── auth.js             — sign in / out, membership gate
├── active-org.js       — tracks active business + active subscription in localStorage
├── active-session.js   — floating "Resume Planning Session" pill
├── active-meeting.js   — floating green "Meeting in progress" pill (v0.5.207)
├── mobile-keyboard.js  — scroll focused input above iOS keyboard (v0.5.130)
└── supabase.js         — Supabase client + dashboard helpers

supabase/
├── schema.sql          — full source-of-truth schema
└── v0.5.NNN-delta.sql  — per-version migration files (run in order on first apply)
```

## Key Rules

### Pages and routing
- All HTML pages use Design 1 (`css/style.css`) — no exceptions.
- Strategy worksheets and Operations tools live at root and ARE the source-of-truth pages. `learn/` is reserved for reference / how-to content (guides, exercises like Values Discovery). `strategy.html` and `operations.html` cards must link to root URLs, not `learn/`.
- Bottom nav order is always: **Home / Planning / Strategy / Operations / Learn / Account** (v0.5.142+) — active item gets `.active` class. Account item links to `index.html`.
- Account-level pages use the white `.screen-toolbar` pattern (toolbar-back + toolbar-title); business-level pages use the navy `site-header` with `← Back · Your Business Coach · 🏢 [Business Name] pill`.

### Conventions every page must follow
- **Sign-out button** canonical ID: `id="signOutBtn"` (camelCase). Canonical class: `class="sign-out-btn"` (kebab-case). Both forms used together. JS lookups use `document.getElementById('signOutBtn')`.
- **Debounced text inputs** that auto-save (attendance, large textareas) use `setTimeout` debounce of **300ms** before writing. Checkbox toggles and button clicks save immediately.
- **Auto-resizing textareas** in worksheets use the `autoGrow(textarea)` helper (resize to `scrollHeight` on load + every keystroke).
- **No Google Fonts anywhere.** The Aptos system stack inherits from `css/style.css`.
- **Active organisation** is read via `window.activeOrg.get()` (exposes the current business id) and `window.activeOrg.getName()`. Set with `window.activeOrg.set(orgId, name)` when navigating into a business. Clears via `clear()`.
- **Active subscription** (multi-tenant): every account-level page that queries businesses MUST filter by the active subscription, otherwise it leaks data across client accounts. Pattern:
  ```js
  const subId = (window.activeOrg && window.activeOrg.getSubscription)
    ? window.activeOrg.getSubscription() : null;
  const memQuery = sb.from('team_members')
    .select('organisation_id, organisations!inner(id, name, subscription_id)')
    .eq('user_id', user.id).eq('status', 'active');
  if (subId) memQuery.eq('organisations.subscription_id', subId);
  const { data: mems } = await memQuery;
  ```
  The page must also load `<script src="js/active-org.js" defer></script>` for `window.activeOrg` to exist.

### Data layer (Supabase, organisation-scoped)
The localStorage-only stub is gone. Strategy worksheets, operations tools, planning sessions, and team check-ins all read/write Supabase, scoped by `organisation_id`. Tables (all in the `public` schema, all gated by RLS):

- **Strategy** — `core_values`, `core_focus`, `targets`, `marketing_strategy`, `leadership_team_members`, `financial_periods` (monthly revenue + expenses; 24-month window on the one-page plan)
- **Operations** — `scorecard_metrics`, `scorecard_entries`, `rocks`, `issues`, `meetings`, `meeting_headlines`, `meeting_todos`
- **Planning** — `annual_sessions`, `quarterly_sessions`, `team_checkins`
- **Multi-tenant** — `subscriptions`, `organisations`, `team_members`, `users`

RLS: any active team member can read; admins + coaches can write. `team_checkins` are readable by any active member of the org (aggregated results are transparent to the team) and INSERT-able by the submitting member.

LocalStorage is still used for:
- **Active org / subscription**: `coach4u_active_org_id`, `coach4u_active_org_name`, `coach4u_active_subscription_id`, `coach4u_active_subscription_name` (set by `js/active-org.js`)
- **Active planning session** (Resume pill): `coach4u_active_planning_session`
- **Operations demo stubs** (`coach4u_demo_meetings`, `coach4u_demo_rocks`, `coach4u_demo_scorecard`) — only `operations.html`'s "Run Weekly Meeting" button still reads `coach4u_demo_meetings`; the rest are vestigial. Slated for removal after the operations migration is fully verified.

## Multi-Tenant (v0.5.111+)
One user can own **multiple `subscriptions`** (one per client account). Active subscription tracked in localStorage as `coach4u_active_subscription_id`. UI:

- **Account switcher** at the top of `index.html` — `<select>` of every subscription the user owns + a `+ New client account` button (teal, top-right).
- **`+ New client account`** modal calls `create_client_account(account_name, business_name)` RPC → creates a new subscription + first business + admin team_member in one transaction, returns `(subscription_id, organisation_id)`. The new business becomes active and the user lands in `business.html`.
- **`+ New Business`** on the account dashboard calls `bootstrap_organisation(business_name, subscription_id)` — `subscription_id` is the currently-active sub, so the new biz lands in the right account.
- **Switching account** — the `<select>` change handler sets `setSubscription(...)`, clears the active org cache, and reloads. The membership query on the account dashboard scopes via `.eq('organisations.subscription_id', activeSub.id)` so each account's businesses are isolated.

Every `account-*.html` carousel page applies the same subscription scoping (v0.5.116). Without it the carousel would mix businesses across every account the user owns.

## Coach + Client Architecture

> **Read this before changing anything about Cath's account setup.** Source of truth — do not guess.

### Two logins — two different purposes

| Email | Used for | User ID |
|---|---|---|
| `cath@coachingwithcath.com.au` | **yourbusinesscoach** (the coaching app) | `f8327825-0022-4c16-b735-cd1c90897889` |
| `cath@coach4u.com.au` | **admin panel only** (coach4uapp-dashboard) | `59066d37-7ddd-4e3e-9efd-67076060b253` |

**Never confuse these.** The `coach4u.com.au` login has no subscriptions with businesses — it is admin-only. All coaching app data belongs to `coachingwithcath.com.au`.

### One subscription (owned by coachingwithcath.com.au)

| Subscription name | `subscription_type` | Contents |
|---|---|---|
| Coaching with Cath | `'coach'` | All businesses — unlimited, no billing |

All 7 businesses live inside the single coach subscription. SARUBA and IAS are **not** separate subscriptions — they are businesses within Coaching with Cath. No billing applies because the subscription is coach type.

**SARUBA businesses:** Coach4U Development, Coach4u, ABMS
**IAS businesses:** IASHQ (parent) → IAS General, IAS Life, IAS Outsourcing

The account switcher shows only one entry. If clients later need their own separate account, their organisations can be moved to a new subscription and all data (rocks, issues, scorecard, meetings etc.) moves with them automatically since it is scoped by organisation ID.

### IAS parent/child structure (depth-1 tree)
- IASHQ — parent
  - IAS General — child
  - IAS Life — child
  - IAS Outsourcing — child

### IAS client data — seeded from May 2026 planning meeting (applied ✓)

Two SQL migrations were run in v0.5.231–232 from the May 2026 IAS planning meeting notes.
Both have been applied to the Supabase DB (`eekefsuaefgpqmjdyniy`) — data is live.

**`supabase/v0.5.231-delta.sql`** — IASHQ only:
- `targets.ten_year` — 10-year vision (by 30/06/2036: self-sustaining business, succession / sale readiness, Jo reducing involvement)
- `targets.five_year` — 7-year plan (by 30/06/2033: $2.5M turnover, D&C loan paid, 15% margin, succession plan)
- 6 current `issues` — cash flow visibility, compliance gaps, staffing/Sunny, cost-splitting, Jo wage discrepancy (Xero 40% vs agreed 30%+10% IASO), tech-tool challenges
- 4 future `issues` — budget finalisation, training plans (Lisa/Fhevy), AI direction, PM protocols
- 5 Q2 2026 `rocks` — group budget, compliance docs, accountability charts, cash flow reporting, PM protocols

**`supabase/v0.5.232-delta.sql`** — all 4 IAS orgs:
| | Core Focus | One-Year Goals | Leadership Team | Issues |
|---|---|---|---|---|
| **IASHQ** | Holding co / ops leadership | FY26–27 wage framework + succession milestones | Jo (30% HQ/ops → transitioning), Leah (60% HQ), Teresa (10% HQ) | — |
| **GI** | GI broking, ~$1M niche | ~$1M revenue, ICS broker hire, SA hire, compliance, ANZIIF for Lisa | Teresa (90%), Jo (10%), Leah (10%), Lisa (dev) | 4 issues |
| **Life/FP** | Life insurance + FP fees, succession focus | Replace Sunny, grow risk/FP revenue, Jo succession begin | Jo (50% → reducing), Leah (10%), Fhevy (dev) | 4 issues |
| **Outsourcing** | Outsourced admin + broking support | Cover expenses, formalise $30k shared resource, confirm GI/Life allocations | Leah (20%), Jo (10%) | 3 issues |

Both files use `WHERE NOT EXISTS` and `ON CONFLICT DO UPDATE` — safe to re-run if needed.

### What NOT to do
- Do not create separate subscriptions for SARUBA or IAS — all businesses belong in Coaching with Cath
- Do not change `owner_user_id` on the Coaching with Cath subscription
- The v0.5.227-delta.sql Part 2 (inserting Cath as team member in non-owned orgs) should NOT be run
- Do NOT re-run v0.5.231 or v0.5.232 — both have been applied. Issues and rocks have no unique constraints; re-running will create duplicate rows

## Login Page Standard (Gold Standard v2.2)

`login.html` uses gold standard — no inline `<style>` blocks, no Google Fonts, `css/style.css` handles all login styling.

Required `<head>` structure:
```html
<link rel="manifest" href="manifest.json">
<meta name="theme-color" content="#003366">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Your Business Coach">
<link rel="stylesheet" href="css/style.css">
```

Post-login redirect: `index.html` (root portal). `login.html` honours a same-origin `?returnTo=` param (used by `team-checkin.html` to bounce the user back after sign-in).

## Supabase
Single project for auth + database. Every page uses the same client:
- URL: `https://eekefsuaefgpqmjdyniy.supabase.co`
- Anon key (publishable): `sb_publishable_pcXHwQVMpvEojb4K3afEMw_RMvgZM-Y`

The anon key is intentionally exposed — it's a `sb_publishable_*` key, designed for client-side use. Security is enforced via Supabase **Row Level Security (RLS)** policies on each table. **Never commit the `service_role` key** (server-side secret).

Always use unversioned import:
```js
import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm';
```

## App URLs
- **Production** (only): `https://cathcoach4u.github.io/yourbusinesscoach/`
- No staging or branch preview URLs. GitHub Pages deploys `main` directly on every push.

## Current Version
v0.5.243

## Pending Actions (must be done outside Claude Code)

### SQL migrations to run in Supabase SQL Editor (`eekefsuaefgpqmjdyniy`)
1. **`supabase/v0.5.235-delta.sql`** — Migrate IASHQ quarterly goals from "Q2 2026" (calendar) to "Q1 2026/27" (IAS fiscal year). Run once after deploying v0.5.235.
2. **`supabase/v0.5.231-delta.sql`** — IAS planning meeting data into IASHQ (targets, issues, rocks). Run if not already applied.
3. **`supabase/v0.5.232-delta.sql`** — Full IAS data across all 4 orgs. Run if not already applied.

## Latest
- **v0.5.243** — User card consistency + spacing tightened across all three account tabs. **(1) Email consistency on user cards**: `account-users.html` now queries the `users` mirror table to fetch emails for all signed-up team members (not just the current user). Previously, members whose `invited_email` was cleared after signup (everyone who joined via an invite) showed as `(unnamed teammate)` with no identifying info — inconsistent with Cath's card which showed her email. Now every card shows an email as the identifying subtitle (unless a display name is set that already is the email). `nameDisplay` also falls back to the email from the users table before hitting `(unnamed teammate)`. **(2) Spacing**: `acct-switcher` padding reduced (16/14px → 12/10px, margin-bottom 6px → 0), `page-header` padding-top 20px → 14px, `page-sub` margin-bottom 4px → 0, `section` margin-top 16-22px → 14px — applied to all three account tabs (index.html, account-users.html, account-setup.html). No SQL.
- **v0.5.242** — Account name no longer shows as a dropdown when there's only one account. All three account tabs (Businesses, Users, Setup) now render the account name as plain bold text for single-account users. The `<select>` switcher only appears when the user owns 2+ subscriptions (e.g. Cath as a coach with multiple client accounts). No SQL.
- **v0.5.241** — Three account-tab polish fixes. **(1) Tax ID label** on `account-setup.html` changed from "Tax ID (ABN / VAT / company number)" to "ABN / ACN" with help text updated to "Your Australian Business Number or Company Number — appears on invoices." (Australia uses ABN/ACN, not VAT). **(2) Spacing** on `account-setup.html` tightened to match `account-users.html`: `page-header` padding-bottom 8px → 0, `page-sub` margin-bottom 12px → 4px, `section` margin-top 22px → 16px — eliminates the large gap between the page description and the first section. **(3) Duplicate email** on `account-users.html` user cards: when a user has no `display_name`, the card title falls back to their email — the `.user-email` row below was also showing the same email string. Fixed with `emailDisplay !== nameDisplay` guard so the email subtitle only renders when it differs from the display name. No SQL.
- **v0.5.240** — Login loop fix on `index.html`. The auth/profile checks at the top of the script were missing `return` statements, so any Supabase query failure (expired token, RLS, transient network error) let execution fall through into `loadAll()`, the subscriptions query also failed silently, `allSubs = []`, and the setup.html redirect overwrote the intended inactive.html redirect — sending the user to the "add a new business" wizard instead of login. Fix: moved auth gate + profile check + signout listener into a proper `async boot()` function (mirrors `account-users.html`'s pattern) with explicit `return` after each redirect. Also added error handling to the subscriptions query in `loadAll()` — an error now bounces to login.html instead of setup.html. No SQL.
- **v0.5.235** — Fiscal year-aware quarterly goals. **(1)** `goals.html` now reads `business_cadence.last_annual_planning_date` (or `annual_planning_date`) to determine the fiscal year start month, then labels quarters as `Q1 2026/27` style instead of calendar `Q2 2026`. Quarter selector shows a ±1-quarter window around the current fiscal quarter, plus any existing values from the DB so old records remain selectable. **(2)** `business.html` dashboard uses the same fiscal quarter for the Quarterly Goals panel and when creating/finding this week's meeting. **(3)** `run-meeting.html` fetches cadence in init and uses fiscal quarter for the inline Priorities Review (Section 3) rocks query. **(4)** `one-page-operations.html` fetches cadence before the rocks query so the Quarterly Goals column matches the fiscal quarter. **(5)** SQL: `supabase/v0.5.235-delta.sql` updates the 5 IASHQ rocks seeded in v0.5.231 from `quarter = 'Q2 2026'` → `'Q1 2026/27'` — run this in Supabase SQL Editor.
- **v0.5.234** — Dashboard panel reorder: Core Values moved to immediately after the quick-action buttons (below "View One-Page Operations"), above the 1-Year Goal panel. No SQL.
- **v0.5.233** — Dashboard + cadence fixes. **(1)** 1-Year Goal "Edit ›" link fixed: was pointing at `targets.html`, now correctly links to `twelve-month-goal.html`. **(2)** Core Values panel moved above Planning Cadence on home dashboard. **(3)** "Year Flow" panel renamed to "Planning Cadence". **(4)** Cadence date input bug fixed: quarterly (and all) date fields now use `change` event only instead of both `input` + `change` — Chrome's date input returns `el.value=""` during partial entry, so the old `input` handler was triggering premature debounced saves with null values mid-typing. Also skipped unnecessary `applyModeUI()` call on `empty → own` first-save transition. No SQL.
- **v0.5.232** — Comprehensive IAS data import Part 2. Creates `supabase/v0.5.232-delta.sql` populating all 4 IAS orgs (IASHQ, GI, Life, Outsourcing) from the May 2026 planning meeting: `core_focus` (purpose + niche), `one_year_goals` in targets, leadership team members (Jo/Leah/Teresa with % time allocations per unit, plus Lisa in GI and Fhevy in Life/FP), and per-unit issues for GI, Life and Outsourcing. SQL only — run `v0.5.232-delta.sql` in Supabase SQL Editor to apply.
- **v0.5.231** — IAS planning meeting notes imported into IASHQ. Created `supabase/v0.5.231-delta.sql` to import: 10-year + 7-year (5-year field) vision targets, 6 current operational issues, 4 future/unresolved issues, and 5 Q2 2026 quarterly goals. SQL only — run `v0.5.231-delta.sql` in Supabase SQL Editor to apply. Ran SQL to move all SARUBA and IAS organisations into the Coaching with Cath subscription, then deleted the now-empty Saruba and IAS subscriptions. All 7 businesses (Coach4U Development, Coach4u, ABMS + IASHQ + IAS General + IAS Life + IAS Outsourcing) now live under one coach account with no billing limits. Updated CLAUDE.md Coach + Client Architecture section to reflect single-subscription model. No code changes.
- **v0.5.229** — DB setup complete + CLAUDE.md corrected. Ran SQL to: (1) fix Saruba subscription_type from `'owner'` → `'business'`; (2) create **Coaching with Cath** subscription (subscription_type=`'coach'`) owned by coachingwithcath.com.au; (3) create **IAS** subscription with IASHQ parent + IAS General / IAS Life / IAS Outsourcing children; (4) add cath@coachingwithcath.com.au as admin team member of all 4 IAS orgs. Updated Coach + Client Architecture section in CLAUDE.md — corrected the two-login setup: `coachingwithcath.com.au` = yourbusinesscoach login (owns all 3 subscriptions), `coach4u.com.au` = admin panel only. No code changes.
- **v0.5.228** — Architecture correction + diagnostic SQL. Added `## Coach + Client Architecture` section to CLAUDE.md documenting that Cath owns all 3 subscriptions (Coach4U / SARUBA / IAS) and that SARUBA/IAS clients have no separate logins — SARUBA and IAS appear in the switcher via `owner_user_id`, not via team membership. Created `supabase/v0.5.228-diagnostic.sql` to check why SARUBA/IAS may be missing (wrong owner or deleted) with fix SQL included. Rewrote `supabase/v0.5.226-coach-setup.sql` to require identifying the correct subscription ID first rather than blanket-updating all owned subscriptions. No code changes.
- **v0.5.227** — Coach account switcher: `index.html` `loadAll()` now also queries `team_members` for role='coach' entries, extracts unique subscription IDs from those orgs, and fetches those subscriptions (possible via new RLS policy). They appear in the account switcher prefixed "Client: NAME". SQL migration `v0.5.227-delta.sql` adds the RLS policy and inserts Cath as a coach team member in SARUBA's and IAS's organisations (matched via `LIKE '%saruba%'` / `LIKE '%ias%'` on subscription name). Must run both v0.5.226-coach-setup.sql and v0.5.227-delta.sql in Supabase SQL Editor.
- **v0.5.226** — Coach account support in account-setup.html. `loadSub` now fetches `subscription_type`. `renderPlanCard()` detects `subscription_type === 'coach'` and renders a "Coach Account" card (role badge, own businesses listed, no billing limit shown) instead of the standard Subscription card. SQL migration `v0.5.226-coach-setup.sql` sets Cath's subscription to `subscription_type = 'coach'` and `included_businesses = 99`. Once run, her card moves from Business Subscriptions → Coaches section in admin.html. SARUBA and IAS to be set up as separate client accounts via "New client account" in yourbusinesscoach index.html.
- **v0.5.225** — account-setup.html: removed auto-save debounce; added Subscription summary card with business count and parent/child tree. — Trial-release hardening pass. Five things bundled. **(1) Service worker paths fixed** — `sw.js` STATIC_ASSETS array still pointed at `/external-Coach4u-app/…` (a previous repo path). PWA install + offline mode were both broken: cache.addAll silently failed, offline fallback navigated to a 404. Rewrote sw.js with relative paths (`./`, `./offline.html`, etc.) so it works on GitHub Pages (`/yourbusinesscoach/`) and any local subpath. Also dropped the legacy `/api/` cache branch (no in-app API calls hit that path; all DB calls go to Supabase directly). Cache-version bump to `coach4u-v0.5.222` invalidates every previous cache so users get the fix automatically on next load. **(2) Legacy account hubs deleted** — `account-strategy.html`, `account-operations.html`, `account-planning.html` were dead since v0.5.146 (only reachable via dead `if (back)` querySelectors on the session list pages). All three files removed. Dead lines stripped from `annual-sessions.html`, `quarterly-sessions.html`, `team-checkins.html`. **(3) Data export** — new `📥 Data Export` card on `account-setup.html` listing every business in the active subscription with a `⬇ Export JSON` button. One click downloads a complete snapshot: strategy worksheets, financial periods, scorecard metrics + entries, rocks, issues, meetings + headlines + todos, planning sessions, team check-ins, business cadence, team members, org metadata. Format: `coach4u-<biz-name>-YYYY-MM-DD.json` with `schema: coach4u-business-snapshot/v1` for forward-compat. Uses RLS so admins/coaches/members all see only the data they're allowed to. **(4) Group Financials rollup** — `account-plans.html` now shows a **💰 Group Financials** banner above the carousel summing revenue + expenses + profit across every business in the account (Last 12 / Next 12). Only renders when there are 2+ businesses AND any have financial data. Hidden in print mode (each per-business card already prints its own financials). **(5) Empty-state copy upgrades** — `goals.html`, `scorecard.html`, `meeting.html` empty states now point at the matching Learn vault guide instead of just saying "Nothing here". **(6) Swipe-to-delete on To-Dos** — new `js/swipe-delete.js` helper, opt-in via `.swipeable` class. Touch-only (desktop unchanged). Swipe a todo row left → reveals a red "Delete" backdrop → past 50% commits the delete (triggers the existing `.todo-del` click handler so the same Supabase delete + optimistic UI path runs). Issues + scorecard rows not yet opted in (issues has a competing click-to-edit handler; would need conflict resolution before turning on). **No SQL.**
- **v0.5.221** — Bugfix: "Meeting in progress" pill reappeared after completing the meeting. User: "Meeting in progress pill is still appearing even though I have completed it. Fix bug." **Root cause**: `js/active-meeting.js` called `autoSetFromMeetingPage()` from inside `render()`. The status-dropdown handler in `run-meeting.html` calls `window.activeMeeting.clear()` when status flips to completed — that runs `clearKey()` (removes localStorage) then `render()`. But `render()` immediately re-seeded the pill via `autoSetFromMeetingPage()` because we were still on `run-meeting.html?id=X`. The page itself doesn't render the pill (the on-matching-meeting guard at line 139 hides it), so the bug was invisible until the user navigated away — then the next page found the re-seeded entry in localStorage and rendered the pill. **Fix**: moved the `autoSetFromMeetingPage()` call out of `render()` and into `init()` only. autoSet still fires once when the page loads (preserving v0.5.213's deep-link defence), but is no longer re-fired on every clear/storage-event/render cycle. Net effect: completing the meeting now properly clears the pill across all pages. **No SQL.**
- **v0.5.220** — Batch audit cleanup + .claude/ scaffolding. **(1) Schema sync** — `supabase/schema.sql` had drifted: missing the `business_cadence` table (v0.5.192/193/197/211), missing `issues.category` + CHECK + index (v0.5.183), missing `organisations.parent_organisation_id` (v0.5.145) + `org_chart_url` (v0.5.187), missing `targets.five_year` (v0.5.181), missing `quarterly_sessions.notes/external_links/commitments` (v0.5.211), missing children-read-parent inheritance policies (v0.5.145/197), missing `"members add issues"` INSERT policy (v0.5.216). Added a new **§14 POST-v0.5.132 ADDITIONS** section at the bottom of schema.sql that catches everything up so a fresh `psql -f schema.sql` produces production schema. All ALTER/CREATE use `IF NOT EXISTS` + `DROP POLICY IF EXISTS` + `DO $$ ... pg_constraint` guards so it's idempotent on top of a populated DB too. **(2) Learn vault audit** — Strategy section reordered to match the Strategy hub: Financials → Core Values → Core Focus → Marketing Strategy → Organisational Chart → Targets → 12 Month Goal (was Core Values first, Financials buried in the middle). Renamed "Leadership Team" card → "Organisational Chart" to match the hub. Operations section added a 5th card **✅ To-Do List** linking to `todos.html` (was missing — v0.5.204 added the page but never surfaced in Learn). Section count bumped 4 → 5 activities. **(3) Guide content sync** — `learn/weekly-team-meeting.html` Common Pitfalls bullet 3 still referenced "the rating" (the rating was removed in v0.5.207); rewrote to "the quick what-worked / what-to-change round". Three guides still used **"1-Year Plan"** terminology that was renamed to **"12-Month Goal"** in v0.5.184: `learn/targets.html`, `learn/quarterly-goals.html`, `learn/annual-planning-session.html` — `replace_all` updated. **(4) Floating-pill coverage** — added `<script src="js/active-session.js">` + `<script src="js/active-meeting.js">` defers to the 4 account-* carousel pages that were missing them: `account-annual.html`, `account-quarterly.html`, `account-checkins.html`, `account-meetings.html`. So the floating green meeting pill + Resume planning pill now follow the user across every page (was 30 pages → now 34). **(5) CLAUDE.md file structure** updated to include `cadence.html`, `twelve-month-goal.html`, `todos.html` + the `active-meeting.js` and `mobile-keyboard.js` JS helpers. **(6) .claude/ scaffolding** — new `.claude/skills/version-bump/SKILL.md` codifies the 7-file dance (VERSION → sw.js → 4 footers → CLAUDE.md → CHANGELOG.md) so Claude can call the skill instead of re-deriving the procedure every release. New `.claude/hooks/session-start.sh` (executable) prints the current version + last 2 CHANGELOG entries at session start; wired via `.claude/settings.local.json`'s `hooks.SessionStart` config. **Requires SQL** if you have a DB that pre-dates v0.5.197 — otherwise the schema.sql additions are all `IF NOT EXISTS` no-ops.
- **v0.5.219** — Meeting timer button is now Pause/Resume only. User: "Can there be a pause meeting rather than end meeting. As there is a completed option" + "Yes put in the simpler version." Reworked the timer in `run-meeting.html`: the button no longer changes meeting status (status dropdown is the only thing that does that). On page load with `status === 'in_progress'`, the button shows **⏸ Pause** with the ticker running anchored on `m.created_at`. Tap Pause → ticker stops, button becomes **▶ Resume**. Tap Resume → ticker snaps to real elapsed since created_at and resumes. The pause state is in-memory only (a reload jumps the displayed time back to actual elapsed since the meeting was created — simpler than persisting pause across navigation). When status is set to **Completed** via the dropdown (only path now), the entire timer bar hides and the interval is cleared. If the user flips back from Completed → In Progress via the dropdown, the timer bar re-shows + re-renders. Removed all of the old `_timerStart` tracking + the "Start Meeting" / "End Meeting" / supabase status writes from the button click handler — those were always confusing duplicates of the dropdown. Default HTML button text updated from "▶ Start Meeting" to "⏸ Pause" (JS overwrites this on load regardless). **No SQL.**
- **v0.5.218** — Three asks. User: "Annual sessions still have the option for scheduled. Remove this. Just want in progress or completed. (Check quarterly). Also I notice that when I go back to the meeting with the pill does this then stop the active meeting. I have to start the meeting again." + "Heading doesn't look great on phone" (screenshot of Role Permissions matrix with ADMIN/COACH/MEMBER labels colliding). **(1) Annual + quarterly sessions: Scheduled status removed.** Same change as v0.5.209 did for meetings. `run-annual-session.html` + `run-quarterly-session.html` status dropdowns now only show **In Progress** / **Completed**. Selected logic: `status !== 'completed'` shows as In Progress (historical 'scheduled' rows display as In Progress). New inserts in `annual-sessions.html` + `quarterly-sessions.html` default to `status: 'in_progress'`. DB CHECK on `*_sessions.status` untouched. **(2) Meeting timer state syncs on return via pill.** Before: tapping the green pill brought the user back to run-meeting.html but the timer button showed "▶ Start Meeting" — the user thought they had to start over. Fix in `renderDetail`: if `m.status === 'in_progress'` AND `m.created_at` exists, set `_timerStart = new Date(m.created_at).getTime()`, show the timer-elapsed span, set the button to "⏹ End Meeting" with the running class, and start the interval ticker. So returning to an in-progress meeting via pill / refresh / direct URL now shows the elapsed time + End button immediately — no fake "start over" prompt. **(3) Role Permissions matrix mobile fix.** On ≤480px viewport the role header columns were 46px wide and the labels "ADMIN" / "COACH" / "MEMBER" collided into "ADMINCOACHMEMBE". Bumped to 58px each + dropped header font-size 0.62 → 0.6rem + tightened letter-spacing 0.5 → 0.2px. New ≤380px breakpoint for ultra-narrow phones drops headers further to 0.55rem / 0.1px spacing + 52px columns. **No SQL.**
- **v0.5.217** — Role Permissions reference table + members can no longer Edit issues. User: "Add a and b" (a = role permissions table in admin area; b = hide edit option for members) + "Remove option for edit." **(a) `account-users.html`** gets a new **🔐 Role Permissions** section at the bottom — a 4-column grid (Permission / Admin / Coach / Member) with green ✓ / grey — marks for each row. Grouped by category: Read, Edit data, Team management, Business, Billing. 14 rows covering everything from "Read all data" → "Counts against billing seats". Sub-note at the end explains security is enforced at the DB layer via Supabase RLS, not just in the UI. Responsive — narrower columns on phones (≤ 480px). **(b) `issues.html`** — now queries the current user's `team_members.role` for the active org in `loadUserRole()` and gates the UI accordingly: if `role !== 'admin' && role !== 'coach'` (i.e., Member), the click-to-edit handler on issue cards is **not attached** + cursor stays default (visually they look readable but non-interactive); the modal's **12 Month Issues — long-horizon** category option is hidden (Members can only INSERT current/future per v0.5.216 RLS). Admins/coaches see no change. The Add Issue button + new-issue modal still work for Members on current/future categories. **No SQL** — v0.5.216 already added the INSERT policy; this is just UI gating + the reference table.
- **v0.5.216** — Members can add to Issues List + Future Issues List. User: "I would want team members to be able to add to an area in future issues list. But not delete. And also keep adding to the issues list but not delete." New RLS policy `"members add issues"` on `public.issues` — FOR INSERT WITH CHECK that `organisation_id IN user_org_ids` AND `category IN ('current', 'future')`. Policies combine with OR for the same operation, so: admins/coaches still pass via the unchanged `"admins write issues"` FOR ALL policy (any category, any operation); members trying INSERT current/future pass via the new policy; members trying INSERT yearly → both policies fail → 403; members trying UPDATE/DELETE → only the FOR ALL applies → 403. **`yearly` category** stays admin/coach-only since those are long-horizon items captured during annual planning, not day-to-day adds. **UI not changed** — issues.html still shows the Add button + edit modal to everyone. Members can hit Add freely (works). If a member taps an existing issue to edit, they'll see an RLS error on save — that's clear feedback rather than silently hiding the affordance (which would require role-querying every row render). **Requires SQL** — see `supabase/v0.5.216-delta.sql`.
- **v0.5.215** — Auto-complete meetings left in_progress past their day. User: "What happens if they just leave the meeting open? Does it automatically close?" → "Yes" (to option 2: auto-complete by date). In `business.html`'s `renderDashboard`, immediately after `meetings = meetingsRes.data`, scan for any with `status === 'in_progress'` AND `meeting_date < todayIso`. Stale ones get flipped: local objects patched to `status: 'completed'` (so downstream rendering treats them as complete), then a fire-and-forget `supabase.from('meetings').update({ status: 'completed' }).in('id', staleIds)` writes the change to the DB. If the floating green pill in localStorage was pointing at one of these stale meetings, `window.activeMeeting.clear()` is also called so the pill stops nagging. Today's and future meetings are untouched (`meeting_date >= today` excluded). The auto-complete fires once per dashboard load — so the same row won't be re-updated on subsequent visits (it's already `completed`). Honest signal: a meeting belongs to its day; if today is past that day and nobody ended it, the row gets flipped to reflect reality. **No SQL.**
- **v0.5.214** — Removed the Child Businesses panel from the parent dashboard. User: "Child business on dashboard just taking up real estate. Remove." On `business.html`'s `parentMode` (the rollup view shown only when the active biz has children), the `<div class="dash-panel">` containing **🏢 Child Businesses** + `#childCards` tappable card list is gone. The 22 lines of JS in `renderParentMode` that built the cards (the `children.map(...).innerHTML = ...` block + click handlers calling `window.activeOrg.set + reload`) are also gone. Net effect: parent dashboard now appends only **💰 Group Financials** below the standard layout (was: Group Financials + Child Businesses). The `.child-card` / `.child-card-head` / `.child-card-arrow` / `.child-card-name` / `.child-card-open` / `.child-card-mini` CSS rules are left in place — small, harmless, and would only need to come back if we re-add the panel. **Children are still reachable** via the biz switcher dropdown in the navy header (active-org.js — shows all biz in the active subscription) and via the Account tab in the bottom-nav → index.html (full list with Open buttons). **No SQL.**
- **v0.5.213** — Two scorecard.html bugs from the user. **(1) Pill still missing** on figures (scorecard) after clicking from a weekly meeting — even after v0.5.211's z-index bump. Diagnosis: the pill set in run-meeting.html depends on that page's JS firing successfully + writing to localStorage before the user navigates away; if any of that is delayed (slow network, async loadDetail, sticky service-worker cache), localStorage might not have the meeting data yet when scorecard.html's active-meeting.js reads it. **Fix**: two layers of defence. (a) `business.html`'s `openOrCreateWeeklyMeeting` now pre-sets the pill via `window.activeMeeting.set(meetingId, '', activeId)` BEFORE navigating to `run-meeting.html?id=…` — so the pill is in localStorage from the moment the user taps Run Weekly Meeting, regardless of whether run-meeting's own JS fires. (b) `js/active-meeting.js` adds `autoSetFromMeetingPage()` — when the script runs and detects we're on `run-meeting.html?id=…` with no matching localStorage entry, it seeds one from the URL params (uses active org id from `coach4u_active_org_id`). So entering run-meeting via ANY path (link from meeting.html list, deep link, etc.) sets the pill automatically. **(2) Home button doesn't work** on scorecard.html when navigating from a meeting. Diagnosis: the `.cell-popover` was at z-index 500, the bottom-nav at z-index 100, the popover at z-index 500. When a popover was open (or even just rendered nearby), tapping the bottom-nav home button hit the popover overlay first — the document click handler closes the popover but the tap is consumed, so navigation doesn't happen. **Fix**: dropped `.cell-popover` z-index from 500 → 90 (below bottom-nav's 100). Now bottom-nav taps always reach the anchor first. No SQL.
- **v0.5.212** — Annual session AREAS — fixed stale links + labels for current schema. User: "Have you looked at the annual and quarterly areas for double dates and all areas are linked." Double-dates audit ✓ (fixed in v0.5.210 — both list pages render `session-item-date` once per row; workspaces had the inner duplicate dropped). Area-links audit found 2 stale entries in `run-annual-session.html`'s AREAS array — Area 3 still said "Update 10-Year + 3-Year Outlook" missing the 5-Year that targets.html got in v0.5.181, and Area 4 "Set 1-Year Plan + Q1 Goals" linked to `targets.html` for the 1-year plan even though 12-Month moved to its own page in v0.5.184. **Fix**: Area 3 label → "Update 10-Year, 5-Year + 3-Year Targets" (link unchanged — still `targets.html`); Area 4 → "Set 12-Month Goal + Q1 Goals" with the 1-year link rewritten from `targets.html` to `twelve-month-goal.html`. Also Area 1 "Review Last Year" was the only area with no link at all — now points to `one-page-plan.html` (the doc the team reviews to assess the year). Plus `ACCOUNT_LINK_MAP` extended with `twelve-month-goal.html → account-targets.html` and `one-page-plan.html → account-plans.html` so account-scoped sessions also resolve correctly. **Quarterly session** AREAS are clean (Review Last Quarter / Lessons + Adjustments / Set Next Quarter's Goals — only references goals.html which is correct). **No SQL.**
- **v0.5.211** — Quarterly session parity with annual + meeting pill z-index bump. User: "Yes" (to both queued items from v0.5.210). **(1) Quarterly session workspace** brought to feature parity with the annual session. **Schema** — `supabase/v0.5.211-delta.sql` adds 3 nullable columns to `quarterly_sessions`: `notes text`, `external_links jsonb NOT NULL DEFAULT '[]'`, `commitments jsonb NOT NULL DEFAULT '[]'`. Existing rows keep working (nullable / default fallbacks). No RLS change — existing quarterly_sessions policies cover the new columns. **`run-quarterly-session.html`** — 3 new blocks added between Areas to Cover and Team Check-in: **🔗 Session Resources** (label + URL pairs in `external_links`), **📝 Session Notes** (free-form textarea writing to `notes`, 500ms debounce autosave), **🎯 Personal Commitments** (name + commitment pairs in `commitments`). All borrow the same CSS classes (.row-list / .row-input / .row-add / .link-row / .commit-row / .notes-input) + JS pattern (renderLinks/persistLinks/scheduleLinksSave + renderCommits/persistCommits/scheduleCommitsSave) from the annual session. Empty state copy adjusted for quarterly cadence ("one thing each leader is taking on for the *quarter*" instead of "year"). **(2) Floating meeting pill z-index bump** — `js/active-meeting.js` `.active-meeting-pill { z-index: 210 }` → `z-index: 2100` so the pill sits above modal overlays (z-index 1000) and toasts (z-index 2000) on the edit destination pages (scorecard cell-popover, issues add/edit modal, goals modal). Likely fix for the user's "pill doesn't come up when I click figures" report — the pill was rendering but hidden behind the modal that opens on those pages. **Run SQL**: `supabase/v0.5.211-delta.sql`.
- **v0.5.210** — Cleanups across session workspaces + learn vault. User: "Annual sessions has backlinks. I'm assuming quarterly does as well. It's showing 2 dates. Check quarterly. And also it's not showing the headings and work under each section." Plus a small audit pass on the Learn vault + Operations hub for terminology drift. **Session workspaces** — both `run-annual-session.html` and `run-quarterly-session.html` had the same issues the meeting workspace had pre-v0.5.207/v0.5.209: a `← Annual Sessions` / `← Quarterly Sessions` back link in the navy header (now removed — bottom-nav Planning tab covers the destination), and a duplicate date showing in the inner `session-detail-title` next to the status dropdown (now dropped; the page header `ws-title` is the single source of truth, status dropdown sits alone right-aligned above the timer bar). Both fixes free up vertical space so the Attendance + Areas + (annual: Resources + Notes + Commitments +) Team Check-in blocks fit more above the fold. **Operations hub** — "Quarter Goals" card renamed to **"Quarterly Goals"** to match the dashboard panel + one-pager column heading + Learn vault card (terminology was drifting; v0.5.184 standardised "12 Month" everywhere, now doing the same for "Quarterly"). **Learn vault** — Strategy section count bumped from `"6 activities"` → `"7 activities"` (12 Month Goal moved to Strategy in v0.5.204 but wasn't surfaced in the vault). New **🎯 12 Month Goal** card added to the Strategy section after Leadership Team, links to the Targets guide + `twelve-month-goal.html` worksheet. **`learn/weekly-team-meeting.html`** — agenda step 7 description was still "Each person rates the meeting 1–10 with one sentence why" (the rating input was removed in v0.5.207); rewrote to "Quick round: what worked today, what should we do differently next week." Coach's tip rewritten to drop the "rates below 8" framing. **Out of scope (queued)** — quarterly session workspace is sparser than annual (no Resources / Notes / Commitments blocks). Bringing it to parity needs a SQL migration adding `notes text` + `external_links jsonb` + `commitments jsonb` columns to `quarterly_sessions` — flagged for a follow-up version. **No SQL this version.**
- **v0.5.209** — Dropped "Scheduled" status + removed back link from meeting workspace. User: "I think the meeting should be in progress or completed. No scheduled" + "I don't think this back link is needed." **(1)** Meeting status dropdown in `run-meeting.html` now has only **In Progress** and **Completed** — Scheduled removed. The CHECK constraint on `meetings.status` still allows `scheduled` (historical rows preserved) — the option just isn't surfaced. Selected-state logic became `status !== 'completed'` (any non-completed meeting shows as In Progress). **(2)** New meeting inserts now default to `status: 'in_progress'` (instead of `'scheduled'`) in 4 places: `business.html` (Run Weekly Meeting button via `openOrCreateWeeklyMeeting`), `todos.html` (`findOrCreateThisWeekMeeting` for the add-todo flow), `meeting.html` (the new-meeting modal). So tapping Run Weekly Meeting immediately marks the meeting In Progress and the floating green pill appears right away. **(3)** Removed the `← Past Meetings` back link from `run-meeting.html`'s navy header. To reach the meetings list now: Operations → Weekly Meetings. Matches the v0.5.189/v0.5.191 audit principle (back link is redundant when bottom-nav already covers the destination — Operations is one tap away). Frees up header space on iPhone where the title was truncated to "Your ..." in the user's screenshot. **No SQL** — `meetings.status` column + CHECK unchanged.
- **v0.5.208** — Annual + quarterly session audit (matches the v0.5.207 weekly-meeting fix). User: "Now go to annual and quarterly sessions and do the same audit and check if it is structured to see the persistence pill and each section can be seen." **Persistence pill audit**: both `run-annual-session.html` and `run-quarterly-session.html` already wire `window.activeSession.set/clear` correctly on the status dropdown change and the timer start/end. But neither was *auto-syncing* the pill on page load — so if the user opened/refreshed an in-progress session, the floating pill stayed in its prior state (could be stale or missing). **Fix**: added a sync call in each file's `loadDetail(id)` right after the session header is set — if `s.status === 'in_progress'` it calls `window.activeSession.set('annual'|'quarterly', s.id, label)`; if `s.status === 'completed'` it calls `clear()`. Matches the same v0.5.207 pattern used in run-meeting.html (which sets the pill from `_currentMeeting.status` on load). **Section visibility audit**: both session pages are vertical-stacked block layouts (Attendance · Areas to Cover · Session Resources · Session Notes · Personal Commitments · Team Check-in for annual; the same minus Notes/Commitments for quarterly) — every section already shows its content inline. Not an accordion like the weekly meeting, so the view + Edit pattern from v0.5.206/v0.5.207 doesn't apply — sections are already visible without expanding. No change needed there. **No SQL.**
- **v0.5.207** — Floating pulsing green "Meeting in progress" pill + To-Do List view+Edit pattern + mobile add-row fix + duplicate date dropped + rating removed. Four user asks bundled: (1) "Can it be that there is green light flashing that can be click on when a meeting is running. ... I just tried it again and I can see the chance to resume the meeting but I want to have something more visible." (2) "I don't think the Todo list I'm weekly meeting is structured to show the items and then the edit button. Can't see the to do well on phone." **(1) New `js/active-meeting.js`** — companion to the existing `active-session.js` planning-session pill. When run-meeting.html loads a meeting that isn't completed, it calls `window.activeMeeting.set(id, label, orgId)` which writes to `localStorage.coach4u_active_meeting`. The script then renders a fixed bottom-centre **bright green pulsing pill** ("● 🗓️ Meeting in progress — Mon 30 May →") on every business-level page (added to all 30 pages that already load active-session.js). Pill animates with a `box-shadow` pulse + an inner-dot ripple. Click → navigates to `run-meeting.html?id=…`. Hide rules: pill doesn't render on the meeting's own workspace page, and hides when the active org doesn't match the meeting's org (so switching businesses doesn't show a misleading pill). The pill is auto-cleared when (a) status set to 'completed' via the dropdown, (b) timer-end button stops the meeting, or (c) the meeting is deleted from `meeting.html`. **(2) Section 5 (To-Do List)** now uses the same view + Edit pattern as sections 2/3/6: shows todos as a clean checklist (descriptions, owner, checkbox still tap-able to mark done — common during-meeting action) with an "Edit Full List ›" link top-right that navigates to `todos.html` (the cross-meeting page from v0.5.204). Inline add row + delete buttons removed from the meeting view — those flows live on todos.html now. Strike-through on completed todos updates in place (no re-render). **(3) Mobile add-row fix** for Section 4 (Customer & Team Highlights): the `.item-add-row` flex container now wraps with `flex-wrap: wrap`; on phones (< 520px) the input takes the full row width and the type-select + Add button drop to a second row. Previously the type-select got truncated to "— Ow" (visible in the user's screenshot). Above 520px the original single-row layout returns. **(4) Duplicate date cleaned** in `run-meeting.html`: the date was rendering twice (page header `ws-title` + inside the agenda card as `meeting-detail-title`). Dropped the inner duplicate; the status dropdown now sits alone right-aligned above the timer bar. **(5) Rating removed** from section 7: title `"Conclude & Rate"` → `"Conclude"`; `renderConclude()` no longer emits the 1-10 rating buttons (only the Notes / Cascading Messages textarea + Save Notes button remain); `wireConclude()` no longer wires rating handlers. The `meetings.rating` column stays in the schema — historical ratings preserved. **No SQL.**
- **v0.5.206** — Weekly meeting agenda sections now show the actual items inline with an "Edit ›" link. User: "Can the weekly meetings be structured so it shows the items and then has an edit button similar to the dashboard. ... if I click on quarterly goal I see the goals and if I need to edit them I click on edit. And this is the same do all of the sections in weekly meeting." Before: sections 2 (Numbers Review), 3 (Priorities Review), and 6 (Issues — Discuss & Resolve) just showed a 1-line paragraph and a link to the dedicated edit page — *"Review quarterly goals. Open Goals →"* — so you'd tap the section, see nothing useful, then have to navigate elsewhere. Now each of those sections shows the actual operational data inline (read-only) with a small "Edit Quarterly Goals ›" / "Edit Weekly Numbers ›" / "Edit Issues List ›" link in the top-right that navigates to the dedicated edit page. **Section 2 (Numbers Review)** — compact scorecard table (metric name + owner + goal + last 6 weeks of values, with hit/miss colouring) identical to the one-page-operations view. **Section 3 (Priorities Review)** — list of this quarter's rocks with description, owner, and status pill. **Section 6 (Issues)** — numbered list of `category='current'` open issues with description + owner. Sections 4 (Customer & Team Highlights), 5 (To-Do List), and 7 (Conclude & Rate) kept their inline-editable UI since the content lives only inside the meeting (no dedicated edit page exists). **Data**: `loadDetail` now fetches scorecard_metrics + rocks + issues in the same Promise.all (3 → 6 queries), then makes a second round-trip for scorecard_entries over the 6-week window (only if metrics exist). Empty arrays fall through to empty-state placeholders. **CSS**: new `.agenda-edit-row` / `.agenda-edit-link` styles for the Edit link, `.agenda-view-list` / `.agenda-view-row` / `.agenda-pill` styles for the read-only lists, `.agenda-sc-table` for the scorecard mini-table. **Helpers**: new `currentQuarter()`, `fmtWeek()`, `fmtVal()`, `editRow()` utility functions. No SQL.
- **v0.5.205** — Dynamic Run Weekly Meeting button label so the user can see the meeting persisted. User: "The weekly meetings I still think it seems to not be working with meetings that when pressing starts it's not persisting and meeting available." Diagnosis: meetings ARE persisting (the insert via wireRunMeetingButton works — RLS allows admin+coach writes), but after v0.5.202 dropped the Next Meeting stat tile and v0.5.204 renamed "This Week" → "Open To-Dos", the home dashboard had NO visible signal that a meeting existed for this week. Tapping "Run Weekly Meeting" a second time silently opened the same meeting (find-or-create), but the unchanged button label made it feel like nothing was saved. **Fix**: in `renderDashboard`, after computing `thisWeekMeeting`, the `#runWeeklyMeetingBtn` label is now set dynamically — `"🗓️ Resume This Week's Meeting"` when a non-completed meeting exists for this Monday, otherwise the default `"🗓️ Run Weekly Meeting"`. Same click handler (`openOrCreateWeeklyMeeting`) — just clearer feedback that the meeting was persisted and is still active. Side benefits: a completed meeting for this week still shows "Run Weekly Meeting" (so the user can start fresh next time), and meetings for *future* weeks (scheduled ahead) don't affect this button (it only checks the Monday of the current week). **No data change** — meetings table + RLS unchanged. **Reachability check**: from Operations → Weekly Meetings → meeting.html, all persisted meetings are still listed normally. The fix is purely about visible feedback on the home dashboard.
- **v0.5.204** — Standalone To-Do List + cross-meeting todo aggregation + 12 Month Goal moved to Strategy. User: "This week's to do isn't showing anywhere. Should this be a box and sql in operations?" → "Yes" + "I think 12 month goal should go from operations to strategic area (at the bottom)." **No SQL** — `meeting_todos` already has everything needed (description, owner, done, due_date, meeting_id). The issue was that the home **"This Week" panel** only queried todos from ONE focused meeting; if there was no meeting yet for this week, the panel was empty even though open todos from prior meetings still needed action. **(1) New page `todos.html`** — lists all open todos across every meeting for the active org, joining via `meetings!inner(organisation_id)` so RLS still filters by org. Each row: 20px checkbox to mark done (optimistic update + rollback on error), description, owner, due-date label ("Due today" / "Due tomorrow" / "Due 5 Jun" / red "Due 30 May" if overdue), "From wk of …" tag showing which meeting it came from. Top of page has an inline add row (description + owner select from team_members + Add button); on submit, the todo is dropped into this week's meeting (created on-demand via the same `findOrCreateThisWeekMeeting` pattern as the home Run Weekly Meeting button). Filter toggle "Show completed" reveals done items; default view is open only. Delete button per row. **(2) Home dashboard — "This Week" panel renamed to "📋 Open To-Dos"** and the JS query now joins `meeting_todos` → `meetings` and filters by org instead of one specific `meeting_id` — so open todos from any meeting stay visible. Limit 5, sorted by due_date (nulls last), then created_at. Panel link `View Meeting ›` → `View All ›` pointing to `todos.html`. The `openOrCreateWeeklyMeeting` click handler is removed from this link (the Run Weekly Meeting button at the top still owns that flow — having it on a "View To-Dos" link was confusing). **(3) Operations hub** — added a **✅ To-Do List** card linking to `todos.html`; removed the **12 Month Goal** card. Order: Quarter Goals → 90 Day Numbers → Issues List → Future Issues List → Weekly Meetings → To-Do List. **(4) Strategy hub** — added a **🎯 12 Month Goal** card at the bottom (after Targets); still links to `twelve-month-goal.html`. So Strategy now reads: Financials → Core Values → Marketing Strategy → Organisational Chart → Targets → 12 Month Goal. No data moved — only the entry-point card switched hubs.
- **v0.5.203** — Renamed weekly-meeting section 1 from "Segue — Good News" to just "Good News". User: "Change 'Segue — Good News'. To just share a good news item." **`run-meeting.html`** — agenda section 1 title changed (`agendaSection(1,'Segue — Good News',...)` → `agendaSection(1,'Good News',...)`). The description inside renderSegue() also simplified from "Start with 1 minute each: share a piece of good news — personal or professional." to "Share a good news item to start the meeting — personal or professional." **`learn/weekly-team-meeting.html`** — the guide's section 1 entry updated to match: "Segue — 5 min. Each person: one personal good news, one business good news." → "Good News — 5 min. Each person shares one piece of good news — personal or professional." No data changes — section 1 has no stored state (it's just an instructional paragraph; the headlines list lives in agenda section 4 "Customer & Team Highlights" via `meeting_headlines`). Pattern continues the v0.5.116 EOS-de-jargoning work (the "Segue" term was EOS-specific).
- **v0.5.202** — Dropped the stat tiles, added an Open Issues panel, renamed "This Quarter" → "Quarterly Goals". User: "Open issues, goals on track, none next meeting not needed on the dashboards home pages. ... wouldn't it just be easier to have the areas visible ie where is says open issues. Can't we just list the issues list. And review quarterly goals. Can it just show the goals." **Removed**: the entire `<div class="stat-row">` block (3 stat tiles: Open Issues count, Goals On Track count, Next Meeting date) + its CSS (`.stat-row`, `.stat-tile`, `.stat-num`, `.stat-label`). The dashboards now show the actual content instead of counts that link to another page. **Added — Open Issues panel** (between Quarterly Goals and This Week): lists the actual `category='current'` open issues for the active org, with description + owner. Empty state: "No open issues. Nice work." New `.issue-row` CSS borrows the rock-row look so the panel feels consistent with Quarterly Goals. **Renamed** the "This Quarter" panel to **🏆 Quarterly Goals** so the title matches what the panel shows (the rock list + progress bar were already there — only the heading changed). **JS**: the count-only `issuesCountRes` query is now `issuesRes` selecting `id, description, owner, status, created_at` — the same fields issues.html fetches. The stat-tile JS (`statOpenIssues`, `statGoalsOnTrack`, `statNextMeeting`, `statNextMeetingTile`) is removed; the `wireMeetingLink(sn)` call too. The Run Weekly Meeting button + View Meeting link in This Week panel still wire through `openOrCreateWeeklyMeeting` from v0.5.201. Net effect: dashboard reads as a flow — Year Flow → 1-Year Goal → Core Values → Quarterly Goals → Open Issues → This Week — no clicks needed to see what's open or who owns what.
- **v0.5.201** — Fixed the "View Meeting" link in This Week + Next Meeting stat tile. User: "The weekly meeting start button and link to go back to meeting isn't working" → clarified "Tap works, but the View Meeting link doesn't" (on both parent + child). Root cause: the `View Meeting ›` link in the This Week panel and the Next Meeting stat tile only navigated to a specific meeting (`run-meeting.html?id=…`) WHEN a meeting already existed for the current week. With no meeting yet, both fell back to their default `href="meeting.html"` (the past-meetings list page), which felt like "broken" because the user expected to land in this week's meeting workspace. The `Run Weekly Meeting` button worked correctly because it always created-or-opened the meeting via `wireRunMeetingButton`. Fix: extracted the create-or-open logic into a shared `openOrCreateWeeklyMeeting(activeId)` helper. `wireRunMeetingButton` now wraps it (no behaviour change). Added a new `wireMeetingLink(el)` helper inside `renderDashboard` that attaches the same click handler to the `thisWeekViewLink` anchor and the `statNextMeetingTile` anchor. Default `href` on both elements stays as `meeting.html` (so middle-click / right-click → "Open in new tab" still navigates to a sensible fallback), but normal taps run `e.preventDefault()` + `openOrCreateWeeklyMeeting`. Net effect: all three home-dashboard meeting entry points (Run Weekly Meeting button + View Meeting link + Next Meeting tile) now behave identically — tap → find this week's meeting if it exists, else create one, then navigate to `run-meeting.html?id=…`.
- **v0.5.200** — Unified home dashboard for parent + child businesses. User: "The home pages should be the same for parent and child. ... Right now the child ones are different" → "Yes do that" (to making them the same with rollup appended for parents). Before: a business with children rendered an entirely separate `parentMode` (Quick actions, Year Flow, Group Strategy, Group Financials, This Quarter's Priorities, Child Businesses) and standardMode was hidden. The parent biz's operational panels (1-Year Goal, This Week, This Quarter, Core Values) were invisible on its own dashboard. Now: **standardMode always renders** for every business — same Stat tiles, Year Flow, 1-Year Goal, Core Values, This Week, This Quarter panels. If the active biz has children, `parentMode` is appended below with **only the rollup-only sections** that don't duplicate standardMode: **Group Financials** (revenue target summed + last-12mo profit across parent + children) and **Child Businesses** (tappable cards to switch active biz). Removed from parentMode (because standardMode already shows the same info for the active biz): the duplicate Quick Actions row, the Year Flow panel (already in standardMode), Group Strategy panel (redundant with 1-Year Goal), This Quarter's Priorities panel (redundant with This Quarter). `renderParentMode` shrinks from 7 parallel queries to 2 (`financial_periods` + `targets` for the rollup totals). The `wireRunMeetingButton('runWeeklyMeetingBtnParent', activeId)` call is gone (button no longer exists). Net effect on IASHQ: same operational dashboard you'd see on IAS General / IAS Life / IAS Outsourcing, plus two extra panels (Group Financials + Child Businesses) at the bottom. No data loss — every removed panel's data is still in the DB and surfaces elsewhere; just isn't duplicated on the home dashboard.
- **v0.5.199** — Dropped the **12 Month Issues** card from the Operations hub. User: "I noticed there is a 12 month issues and an issues list. This doesn't look right" → picked "Drop 12 Month Issues" from the 3 options. Long-horizon issues belong in the annual planning session, not on the day-to-day hub. **`operations.html`** — removed the second activity-card (`href="issues.html?category=yearly"`). New card order on the hub: 12 Month Goal → Quarter Goals → 90 Day Numbers → Issues List → Future Issues List → Weekly Meetings (6 cards, was 7). **No SQL, no data loss** — the `issues.category` column still allows `'yearly'`/`'current'`/`'future'`, existing issues tagged `yearly` are still in the DB, and the add/edit modal's Category dropdown still has the "12 Month Issues — long-horizon" option so users can still classify (or re-classify) issues as yearly. Direct URL `issues.html?category=yearly` still works for anyone with a bookmark; the page header + filter logic still render correctly. Annual planning sessions can still surface yearly issues — they just don't have a top-level hub shortcut anymore.
- **v0.5.198** — Operations one-pager: 4-column flow + 12 Month Goals. User: "Operations one page — Start with 12 month goals, Then issues, Then quarterly goals, Then weekly numbers." **`one-page-operations.html`** rebuilt to a 4-column layout in exactly that order. New first column **12 Month Goals** reads `targets.one_year_goals`, renders as a teal-numbered bullet list (same look as the strategy one-pager). Falls back to the parent's `one_year_goals` when this org has no own row (matches the v0.5.197 cadence inheritance pattern). The column heading shows the plan year computed from `business_cadence` (with same parent fallback) — e.g. `Apr 2026 – Mar 2027`. Grid changed from `1fr 1.55fr 1fr` (3 columns) to `1fr 1fr 1fr 1.4fr` (4 columns, Weekly Numbers gets the widest track since it's a table). `.num-list` CSS rules copied in. Mobile @media block bumps `.num-list` font-size for thumb-reading. `Promise.all` grows from 4 → 5 queries (targets added). **`account-ops-plans.html`** (cross-business ops carousel) gets the same treatment: same 4-column layout, same render order, `targets` + `business_cadence` added to the batch (3 → 5 parallel queries), plus inheritance fallback applied per-org (children pick up the parent's targets + cadence when their own row is empty, since the parent is typically in the same orgIds batch). **Out of scope** — the user also noted "I noticed there is a 12 month issues and an issues list. This doesn't look right" — flagged for clarification but no change yet; need to know whether they want the categories merged, renamed, or kept as-is.
- **v0.5.197** — Planning Cadence inheritance: children inherit parent's dates by default. User: "Will these dates appear on parent and child versions?" + "Yes" (to adding inheritance). Mirrors the v0.5.145 inheritance pattern used for Core Values / Core Focus / Targets. **Schema** — `supabase/v0.5.197-delta.sql` adds one additive SELECT policy on `business_cadence`: a user can read a cadence row if its `organisation_id` is the `parent_organisation_id` of any org they're a member of. No new columns; INSERT/UPDATE/DELETE still require admin+coach on the row's org. **`cadence.html`** — full inheritance UI: detects this org's `parent_organisation_id`, loads own + parent's cadence in parallel, picks a mode (`own` / `inherited` / `override` / `empty`). New banner element at the top of the form: teal **"📥 Inherited from <Parent> — Override locally"** when in inherited mode (inputs disabled, populated with parent's data); amber **"✏️ Overridden locally from <Parent> — Revert to parent"** when in override mode (inputs enabled, populated with own data). The Override button snapshots the parent's row into a new own row; the Revert button deletes the own row and re-renders in inherited mode. Save/scheduleSave are no-ops in inherited mode (defence in depth). **`business.html`** — `loadAndRenderYearFlow` now falls back to the parent's cadence when this org has no own row, so child dashboards transparently show the parent's Year Flow timeline. **`one-page-plan.html`** — plan-year anchor also falls back to parent's `last_annual_planning_date` / `annual_planning_date` for the doc header. **Out of scope** — `account-plans.html` (cross-business strategy carousel) wasn't updated; each business's plan-year label still uses only its own cadence row. Easy follow-up if the user wants children's cards to inherit there too.
- **v0.5.196** — Year Flow panel now shows on the parent dashboard too. User: "This is not appearing on this version." Root cause: v0.5.195 added the `📅 Year Flow` panel inside `#standardMode` only — but the user is on IASHQ which is a parent biz, so the dashboard renders `#parentMode` (with standardMode hidden via `display: none`). The panel + render call were both inside the hidden subtree → invisible. **Fix**: added the same `<div class="dash-panel">` block to `#parentMode` (right after the quick-actions row, before Group Strategy) with a distinct `id="yearFlowParent"`. Refactored `renderYearFlow(cadence, targetId)` to accept a target element id and extracted the cadence fetch into a shared `loadAndRenderYearFlow(orgId, targetId)` helper. `renderDashboard()` calls it with `(orgId, 'yearFlow')`; `renderParentMode()` calls it with `(parentOrgId, 'yearFlowParent')`. The parent dashboard now shows the parent biz's own cadence (the holding company has its own annual + quarterly + weekly rhythm). If you're still not seeing the panel: it may be a service-worker cache — hard refresh (pull down in Safari) to fetch the new sw.js.
- **v0.5.195** — Year Flow panel on the home dashboard. User: "Add the dates on dashboard so it looks like a flow for the year." New `📅 Year Flow` panel inserted into `business.html`'s standardMode between the Stat tiles and 1-Year Goal panels. Renders a vertical timeline of up to 5 events — `Last Annual Planning → Q1 → Q2 → Q3 → Next Annual Planning` — sorted by date, with each row showing a dot, the label, the formatted date (e.g. *5 Jul 2026*) and a status pill (`✓ Done` / `Next up` / `Upcoming`). The "next up" item is highlighted with a filled teal dot + teal halo so the user can see at a glance where they are in the year. Below the timeline, a small `📆 Weekly meeting: Mondays at 9:00am` line rolls up the weekly cadence. Empty states: if no cadence row exists (or the v0.5.192 migration hasn't been applied), the panel shows *"Set your planning cadence to see your year flow → Set it up"* with a teal link to `cadence.html`; if the row exists but no dates are filled in, similar prompt. **Edit link** in the panel head goes to `cadence.html`. Fetched in `renderDashboard()` after the existing Promise.all (separate call so a missing table or empty row doesn't break the rest of the dashboard). Parent-mode (`renderParentMode`) wasn't touched in this version — only the standard own-business view shows the panel.
- **v0.5.194** — Dropped Q4 from the Planning Cadence form. User: "Remove q4 date. The flow is Annual planning then q1, 2, 3." The annual planning session itself acts as the year-end / year-start anchor, so Q4 was double-counting. **`cadence.html`** — Q4 session block (both Day 1 + Day 2 inputs) removed from the Quarterly Sessions card. Card description updated to "Q1, Q2 and Q3 sessions — the annual planning session above replaces Q4 as the year's end + start." `FIELDS` array shrinks from 13 → 11 (drops `q4_session_date` and `q4_session_date_2`). **No SQL** — the `q4_session_date` + `q4_session_date_2` columns stay in the schema (existing data preserved); the form just no longer reads or writes them. If you'd never run v0.5.192/193 SQL yet, those columns are still defined but unused — harmless. Plan-year anchor logic on the one-page plan + carousel unchanged (still keyed by `last_annual_planning_date` first, `annual_planning_date` second).
- **v0.5.193** — Cadence extensions: last-annual date + 2nd-day per session. User: "Add in planning cadence — Last annual planning date. And add 2 dates for every session in case they want to do more than one." **Schema** — `supabase/v0.5.193-delta.sql` adds 6 nullable `date` columns to `business_cadence`: `last_annual_planning_date` (when the most recent annual was held — now anchors the plan year on the One-Page Plan) plus `annual_planning_date_2` and `q1..q4_session_date_2` (optional Day 2 for sessions that run across two days). All inherit existing RLS — no policy change. **`cadence.html`** — Annual Planning card now stacks (1) Last annual planning date, (2) Next annual planning Day 1 + Day 2 (2-col grid); each Quarterly Session row gets the same Day 1 + Day 2 layout. The plan-year summary line prefers `last_annual_planning_date` if set, otherwise uses `annual_planning_date` with the suffix "(anchored from next session)" so it's clear which date is driving the label. **`one-page-plan.html`** — cadence fetch now selects both columns; the year label picks `last_annual_planning_date` first, falls back to `annual_planning_date`, then to the calendar year. **`account-plans.html`** — same upgrade for the cross-business carousel; the per-org `cadence` object now holds both dates and the `planYearLabel(...)` call uses the same precedence.
- **v0.5.192** — Planning Cadence + plan-year anchor on the one-page plan. User: "One page plan dates are 10 years. This isn't correct. It's only ever one year some might start at different times so where does the date get listed" + "In planning I want to be able to allocate dates for annual planning date then the quarterly sessions. Then a day to list the weekly day and time." Two-part change: **(1) Schema** — new `business_cadence` table (one row per organisation, PK = `organisation_id`) with `annual_planning_date date`, `q1_session_date`..`q4_session_date date`, `weekly_meeting_day text` (CHECK against the 7 day strings), `weekly_meeting_time time`, plus `updated_at timestamptz`. RLS: members read, admins+coaches write — same pattern as the other org-scoped tables, using the existing `user_org_ids` + `user_admin_org_ids` helpers. See `supabase/v0.5.192-delta.sql`. **(2) UI** — new **`cadence.html`** with three cards (Annual Planning · Quarterly Sessions · Weekly Team Meeting), all auto-save with 350ms debounce. Inline summaries below each section: the annual date renders as *"Plan year: Apr 2026 – Mar 2027"*; the weekly day+time as *"Weekly meeting: Mondays at 9:00am"*. New **📅 Planning Cadence** card added to the top of `planning.html`'s activity grid. **(3) Plan-year on the one-pager** — `one-page-plan.html` now fetches `business_cadence.annual_planning_date` and renders the doc-header year as the 12-month window starting from that date (e.g. *"Apr 2026 – Mar 2027"*), replacing the misleading `2026 – 2036` (which was `current year + 10`). Falls back to just the current calendar year if no cadence is set. `account-plans.html` (cross-business carousel) does the same — bumps the Promise.all from 6 → 7 queries to include cadence per business, so each business's card shows its own plan year. **Defensive load** — if the migration hasn't been applied yet, the cadence query returns an error which is caught and the label falls back to the calendar year (no broken UI). The `cadence.html` save handler also detects the "table does not exist" error and shows *"Schema not migrated yet — run supabase/v0.5.192-delta.sql"* in the status line.
- **v0.5.191** — Full audit + cleanup of header back links + "Add Leadership Team Member" copy. User: "Those backlinks are not needed? Do full audit for back links" + "In org chart say add leadership team member." Extending the v0.5.189 principle (back link is redundant when the bottom-nav already covers the destination) across the whole app. **Audit verdict:** 21 pages have a `class="header-back"` link; categorised by destination → bottom-nav coverage. **Removed (14 pages — back link goes to a hub the bottom-nav already has):** `core-focus.html`, `core-values.html`, `financials.html`, `leadership-team.html`, `marketing-strategy.html`, `targets.html` (all → strategy.html, covered by Strategy tab) · `goals.html`, `issues.html`, `scorecard.html`, `meeting.html`, `twelve-month-goal.html` (all → operations.html, Operations tab) · `annual-sessions.html`, `quarterly-sessions.html`, `team-checkins.html` (all → planning.html, Planning tab). **Kept (4 workspaces — back link goes to an intermediate list page that the bottom-nav doesn't reach directly):** `run-meeting.html` → meeting.html, `run-annual-session.html` → annual-sessions.html, `run-quarterly-session.html` → quarterly-sessions.html, `run-team-checkin.html` → team-checkins.html. **Kept (3 account-level pages):** `account-strategy.html`, `account-operations.html`, `account-planning.html` — their bottom-nav has only the `🏛️ Account` tab (one item, already-active), so it's not a real navigation rail; the header back-link is the obvious way out. Side effect on `annual-sessions.html` + `quarterly-sessions.html` + `team-checkins.html`: each had `?scope=account` JS that swapped the back-link's text/href to `← Account Planning`; the `if (back) { ... }` guard makes the now-dead querySelector a no-op (no error), but the legacy back-to-account-planning affordance is gone — that's fine since `account-planning.html` was already noted as "legacy" in v0.5.146. **Org chart copy** — `leadership-team.html`'s primary CTA changed from `+ Add Team Member` to `+ Add Leadership Team Member` so it matches the page's purpose as the Organisational Chart (the worksheet page title still reads "Leadership Team" — Strategy hub card label says "Organisational Chart"; the underlying file/table name is preserved for compatibility).
- **v0.5.190** — Two cleanups on the one-page docs. User: "I think the font is different from one page links. Make consistency. Also the financials are on both of them. Only needed on strategy." **(1) Font** — `.plan-page` in all 4 doc files (`one-page-plan.html`, `one-page-operations.html`, `account-plans.html`, `account-ops-plans.html`) was overriding `font-family` with `'Aptos', 'Segoe UI', system-ui, sans-serif`, missing the `-apple-system` + `BlinkMacSystemFont` fallbacks that the site-wide `--font-body` variable in `css/style.css` includes. On iOS this meant the docs picked a slightly different font path than the rest of the app, producing the subtle inconsistency the user spotted. Changed each override to `font-family: var(--font-body)` so the docs now use the exact same stack as every other page. **(2) Financials** — the Financial Outlook strip (Last Year + This Year annual totals) used to render on both the Strategy one-pager AND the Operations one-pager. Removed it from the operations side so financials only show on Strategy. Touches: `one-page-operations.html` (deleted the `.doc-financials` HTML block + `fmtCurrency` / `profitClass` / `aggregateYear` / `renderAnnualFinancials` helpers + the `financial_periods` fetch + the windowing constants — 4 parallel queries → 3) and `account-ops-plans.html` (same removal — deleted `buildAnnualTotalsBlock` + `fmtCurrency` + the per-org `finByYear` accumulator + the `financial_periods` query). The `.doc-financials` / `.fin-strip-*` / `.fin-line*` CSS rules are left in place (small, harmless, and identical to the strategy docs that still use them). The Strategy one-pager and Strategy carousel keep the strip — that's where financial outlook belongs. No SQL.
- **v0.5.189** — Removed the redundant back link from the one-page plan + operations toolbars. User: "The back link isn't needed is it?" After v0.5.188 added the standard 5-tab bottom-nav to both pages, the toolbar's `← Strategy` / `← Operations` back link became redundant — Strategy and Operations are both one tap away in the nav (and Home is too, which the back link never was). Removed from both `one-page-plan.html` and `one-page-operations.html`. Net effect: the toolbar is now `[page title (desktop only) · 🏢 IASHQ]` on the left, `[🖨️ Print]` on the right. On phones (where `.toolbar-title` is hidden) the toolbar collapses to just `🏢 IASHQ · 🖨️ Print` — almost nothing competing for space.
- **v0.5.188** — Added the standard 5-tab bottom-nav to `one-page-plan.html` and `one-page-operations.html`. User: "The link to the one page operations page has a back link to operations. I wonder if it should have the tabs down the bottom like the other pages?" Before, the two presentation docs only had the white sticky toolbar at top with a single `← Operations` / `← Strategy` back link; on phones the doc scrolls long, so from the bottom you had to scroll back up to navigate anywhere else. Both pages now end with the standard `<nav class="bottom-nav">` block (Home · Planning · Strategy · Operations · Learn) — Strategy active on the one-page plan, Operations active on the one-page operations, matching the existing back-link logic. CSS for `.bottom-nav` + `.bottom-nav-item` + `.bottom-nav-icon` inlined in each file (matches the pattern used on the other 22 business-level pages). `body { padding-bottom }` bumped from 24px → 80px so the doc-footer doesn't sit behind the fixed nav. Print is unaffected — the existing `@media print` block now hides `.bottom-nav` alongside `.screen-toolbar` and `.mobile-hint`, so paper output is still just the doc.
- **v0.5.187** — External Org Chart URL on the Leadership Team page + One-Page Plan. User: "Add to organisation chart a way they can add a link to their org chart. This can be created external from app if they want it. Including this link on the one page plan." Many users prefer to build their org chart in a visual tool (Lucidchart, Miro, Google Drawings, Figma, draw.io) rather than describe it as a list of leadership-team rows; this lets them paste that share-link into the app so the whole team can open it in one tap. **`leadership-team.html`** gets a new "🔗 Org Chart Link" card at the top (above "+ Add Team Member"): a URL input + a "👁️ View →" teal button that appears when a URL is set. Auto-saves on input (400ms debounce). URLs without a scheme get `https://` prepended on render so users can paste "example.com/chart" without breaking the link. **`one-page-plan.html`** shows the same link under the Leadership Team field as small teal `🔗 View org chart →` text when set; hidden when empty. **`account-plans.html`** (cross-business carousel) also surfaces the link per business — fetches `org_chart_url` via the existing `organisations!inner(...)` join on the team_members query. **No RLS change** — the existing `organisations` policies cover it (members read, admins + subscription owner write). **Requires SQL** — see `supabase/v0.5.187-delta.sql`: `ALTER TABLE public.organisations ADD COLUMN IF NOT EXISTS org_chart_url text`. Children-read-parent policies for org-level data don't currently extend to organisations metadata; a child business sees only its own org_chart_url (not the parent's). If you want inheritance later, follow the v0.5.145 pattern.
- **v0.5.186** — Tidier toolbar on phones for the one-page plan + ops. User screenshot of `one-page-plan.html` on iPhone showed the white screen-toolbar cramming 4 elements (`← Strategy` + page title + biz pill + Print button) into ~390px width, with the page title truncated to "One-P..." and the biz pill clipped to "I...". Two changes: **(1)** Hide `.toolbar-title` below 600px viewport — the page title ("One-Page Business Plan" / "One-Page Operations") is redundant on a phone because the navy doc-header right below already renders it prominently as `.doc-plan-title` ("ONE-PAGE BUSINESS PLAN" in large letterspaced caps). Dropping it from the toolbar frees enough room for the biz pill ("🏢 IASHQ") to render its full text. **(2)** Trimmed the Print button label from "🖨️ Print / PDF" → "🖨️ Print" everywhere (modern browsers' print dialog always offers "Save as PDF" anyway, so the "/ PDF" was redundant) AND added a smaller phone-only padding (`8px 12px` vs `8px 14px`) + font-size (`0.78rem` vs `0.82rem`) so the button takes less horizontal space on small viewports. Net effect: phone toolbar is now **`← Strategy · 🏢 IASHQ · 🖨️ Print`** with no truncation. Desktop toolbar still shows the page title (no change above 600px).
- **v0.5.185** — One-page plan + one-page operations now have a screen-first layout. User: "I really like the one page printed plan but can this information appear online screen first. ... I just think for those people that prefer to see it on screen." Recommended approach: make the existing `one-page-plan.html` + `one-page-operations.html` responsive (rather than create new screen-only files) — same URL, same data, two presentations, content stays in sync forever. **Change**: added a `@media screen and (max-width: 820px)` block to both files that (a) drops the `min-width: 760px/820px` on `.plan-page` so the doc collapses to viewport width, (b) switches `.doc-body` from `grid-template-columns: 1fr 1.45fr 1fr` to `1fr` so the 3 cards stack vertically, (c) swaps column dividers from right-border to bottom-border, (d) bumps typography across the board for thumb-reading (field labels 0.56→0.66rem, field values 0.76→0.95rem, headings 0.6→0.72rem, pills 0.68→0.82rem, padding 14→18px), (e) stacks the Financial Outlook strip (Last 12/Next 12 or Last Year/This Year) to 1 column, (f) lets the Operations Scorecard table scroll horizontally inside its column so the rest of the doc still stacks normally. The `screen` keyword keeps these rules from applying when printing — the existing `@media print { ... }` block still wins, so Print/PDF still outputs the 4-column landscape A4 unchanged. Also: removed the obsolete "← Scroll right to see the full plan" mobile-hint banner (no longer needed now that the doc fits the viewport), and updated the toolbar hint from "Scroll to see full plan · Print for landscape A4" to "Tap Print to save as PDF or print landscape A4". Entry points (View One-Page Plan / View One-Page Operations quick-action buttons on Home) unchanged.
- **v0.5.184** — Consistent wording + de-duplicated the 12-Month horizon. User: "The yearly /12 month wording needs to be consistent. Does the split make sense over the 2 pages? ... It's not intention to have on both pages." Two changes: **(1) Rename "Yearly Issues" → "12 Month Issues"** so the same time-horizon uses the same words as the "12 Month Goal" card. Touches `operations.html` (card label), `issues.html` (page header for `?category=yearly` + modal dropdown option label). **(2) Move the 12-Month section off Strategy → it now lives only on Operations.** The Targets worksheet on Strategy used to have 4 cards (10-Year / 5-Year / 3-Year / 12-Month) and the Operations "12 Month Goal" card linked to the same `targets.html` — so the user could reach the 12-month goal from both pages. Now: **`targets.html`** drops the 12-Month card (3 cards: 10-Year, 5-Year, 3-Year) and `FIELDS` array shrinks accordingly. **New `twelve-month-goal.html`** — dedicated single-textarea editor that writes to the same `targets.one_year_goals` column the dashboards + one-pagers + carousels already read from. **`operations.html`** 12 Month Goal card now links to `twelve-month-goal.html` instead of `targets.html`. **`strategy.html`** Targets card description trimmed to "10-year, 5-year and 3-year goals". **No SQL** — the `one_year_goals` column stays in the schema (still read by `business.html`, `one-page-plan.html`, `account-plans.html`, `account-targets.html`); just moved which page edits it.
- **v0.5.183** — **Strategy + Operations restructure with a 3-way Issues split.** User: "This is the order for strategy: Financials / Core values / Marketing strategy / Leadership team / Organisational chart" + new Operations layout. **Strategy** (`strategy.html`) reordered to **Financials → Core Values → Marketing Strategy → Organisational Chart → Targets**. The "Leadership Team" card is renamed to **Organisational Chart** (still links to `leadership-team.html` — the underlying worksheet keeps its name). **Core Focus** is removed from Strategy; it moves to Operations as **12 Month Goal** (links to `targets.html`, since that's where the 12-month goal lives). **Operations** (`operations.html`) rebuilt as 7 cards in order: **12 Month Goal** (→ targets.html) · **Yearly Issues** (→ issues.html?category=yearly) · **Quarter Goals** (→ goals.html) · **90 Day Numbers** (→ scorecard.html) · **Issues List** (→ issues.html?category=current) · **Future Issues List** (→ issues.html?category=future) · **Weekly Meetings** (→ meeting.html). The 3 issues cards are a real category split, not a label swap. **`issues.html`** now reads `?category=` from the URL (default `current`), filters its query with `.eq('category', cat)`, swaps its `<h1>` + sub between **Yearly Issues / Issues List / Future Issues List**, and the add/edit modal gets a new **Category** dropdown so an issue can be re-classified later. Callers that surface issue counts/lists at the operational level (`business.html` own + child dashboards, `one-page-operations.html`, `account-ops-plans.html`, `account-issues.html`, `run-meeting.html`'s IDS link) all now filter to `category='current'` so yearly + backlog items don't bleed into the weekly meeting view. **Requires SQL** — see `supabase/v0.5.183-delta.sql`: new `category text NOT NULL DEFAULT 'current'` column on `issues` + CHECK constraint (`'yearly' | 'current' | 'future'`) + `(organisation_id, category)` index. Existing rows backfill to `current` so day-1 looks identical to the old single Issues view.
- **v0.5.182** — Targets card titles trimmed to just the timeframe. User: "Change the targets to each target / 10 year, 5 year etc." Dropped the "Goal" suffix from each card title on `targets.html`: "10-Year Goal" → "10-Year", "5-Year Goal" → "5-Year", "3-Year Goal" → "3-Year", "12-Month Goal" → "12-Month". The section is already called Targets, so each card being labeled with just the timeframe is unambiguous. Card descriptions, placeholders, columns and save logic all unchanged.
- **v0.5.181** — Simplified the Targets worksheet to one text box per timeframe + added a 5-Year Goal. User: "Change targets and move to individual boxes for 10 year goal / 5 year goal / 3 year goal / 12 month goal." `targets.html` rewritten — was 3 cards (10-Year Vision, 3-Year Outlook with date/revenue/profit grid + textarea, 1-Year Plan with date/revenue/profit grid + textarea); now **4 cards each with just one `<textarea>`**: 10-Year Goal → `ten_year`, 5-Year Goal → `five_year` (NEW column), 3-Year Goal → `three_year_desc`, 12-Month Goal → `one_year_goals`. The `FIELDS` array dropped from 9 fields to 4 — only those 4 columns get touched on save. The other columns (`three_year_date`, `three_year_revenue`, `three_year_profit`, `one_year_date`, `one_year_revenue`, `one_year_profit`) **stay in the schema** so existing data isn't destroyed; the worksheet just no longer reads or writes them. Consumer pages (business.html standard + parent modes, one-page-plan.html) that read those legacy columns continue to work — they'll show empty for businesses that never had those values, populated for those that did. **Requires SQL** — see `supabase/v0.5.181-delta.sql` for the `ALTER TABLE … ADD COLUMN five_year text` migration. Children-read-parent RLS already covers the whole row, so no policy change.
- **v0.5.180** — Removed the "Goals on track" line from child cards on the parent dashboard. User: "Remove goals on track in child accounts." After v0.5.179 the child cards were already trimmed to just one metric ("Goals on track: X of N"); this version removes that too, so each child card is now just **↳ Name + Open ›**. The `.child-card-mini` block is gone from the per-card template, and the `.child-card-head { margin-bottom: 10px; }` rule was nulled out since there's no longer a row below the head to space away from. The `rocksPerChild` Map is still built (cheap), it just doesn't render. Child cards click-to-switch behaviour unchanged.
- **v0.5.179** — Slimmed and re-ordered the parent-mode (home) dashboard. User: "Home dashboard. Just list the 12 month goal remove 10 year vision and 3 year / Dashboard - Group financials - remove last 12month expenses and revenue / Order on dashboard: Group strategy / Group financials / This quarters priorities / On the dashboard child companies remove open issues and next meeting." Four changes on `business.html`'s parent-mode (the IASHQ-style dashboard): **(1)** Group Strategy panel now shows ONLY 12-Month Goal — dropped the 10-Year Vision and 3-Year Outlook rows. JS still computes the values but `setText` becomes a no-op when the element isn't in the DOM, so the dead writes don't error. **(2)** Group Financials now shows ONLY 2 cells: 1-Year Revenue Target + Last 12mo Profit. Last-12-month Revenue and Expenses cells removed. `setFin` similarly skips missing elements. **(3)** Panel order changed from Strategy → This Quarter → Financials → Children, to **Strategy → Financials → This Quarter → Children** (financials moved up, just below strategy, since they're the headline numbers users want to glance at). **(4)** Child cards now show ONLY "Goals on track: X of N" — dropped the Open Issues and Next meeting rows. The Supabase queries that populated those fields stay (cheap, and may be used elsewhere), the data just no longer renders. Child-card click handler (tap → switch active org → reload) unchanged.
- **v0.5.178** — Heading alignment across the 5 hubs now consistent. User: "If you look at each page the headings all start at a different spot. Should all be consistent." **Root cause:** `css/style.css` sets a global `.container { padding: 32px 20px; }` (with mobile `@media (max-width: 640px) { .container { padding: 20px 16px; } }`). Four hubs (Home / Planning / Strategy / Operations) only override `padding-bottom: 80px` (or 88px) in their inline `<style>` blocks, so the global top + sides padding stays — heading text rendered ~36px from the left, ~52px from container top (or ~32px/~40px on mobile). `learning-vault.html` used the shorthand `padding: 0 0 90px` which **fully resets** all four sides — its heading rendered at ~16px from the left, ~20px from top. Net result: Learn's heading was 20px to the left and 32px higher than the others. **Fix:** changed the four hubs' container rule from `padding-bottom: Xpx` to `padding: 0 0 Xpx` (full shorthand) so the global padding is fully reset everywhere. The `.ws-header` (and `.vault-header`) `padding: 20px 16px 0` is now the only thing controlling where the heading sits — same on all five hub pages.
- **v0.5.177** — Centered the "Run Weekly Meeting" button on the home page. User: "Run weekly meeting box needs to be centred." The button used `display: flex; justify-content: space-between` with a label-span on the left and a `›` arrow-span on the right — which made the label sit hard-left while the two View One-Page buttons below (which use `text-align: center`) had their labels centered. Inconsistent. Removed the arrow span and switched the CSS to `justify-content: center` + `text-align: center` so the icon + label now sit dead-centre, matching the layout of the View One-Page Plan and View One-Page Operations buttons. Both meeting buttons (standardMode `runWeeklyMeetingBtn` + parentMode `runWeeklyMeetingBtnParent`) updated. The unused `.meeting-btn-arrow` CSS rule also dropped.
- **v0.5.176** — Same heading + sub pattern now on Home (`business.html`) and Learn (`learning-vault.html`). User: "Have home and learn with the same headings." Added `.ws-header` CSS + `<div class="ws-header"><h1>🏠 Home</h1><p>…</p></div>` block at the top of `business.html`'s container, just above the parent/child context row. Home title + sub: *"🏠 Home"* / *"Your business overview at a glance — key numbers, this week's focus, and quarterly priorities."* On `learning-vault.html`, aligned the existing `.vault-header` / `.vault-title` / `.vault-sub` values to match `ws-*` (font-size 1.15rem → 1.2rem, padding 24/16/4 → 20/16/0, sub margin 0 → 0 0 20px, line-height 1.45 → 1.5) and renamed the title from "Learning Vault" → "Learn" so it matches the bottom-nav tab label. Sub copy kept ("A written guide for every activity in your Coach4U program. Read the guide first, then jump into the worksheet."). All five hubs (Home / Planning / Strategy / Operations / Learn) now share the identical visual heading + sub-description pattern.
- **v0.5.175** — Added the planning-page-style heading + sub-description to `strategy.html` and `operations.html`. User: "The planning page has a heading a description. Do the same heading and sub detail under in the operations and operations." Both hubs were sitting straight on top of their activity cards — no page identity, no orienting copy. Inserted a `<div class="ws-header">` block right inside `.container` on each, matching the exact pattern from `planning.html` (same `ws-header` / `ws-title` / `ws-sub` CSS rules). **Strategy:** `🧭 Strategy` + sub *"The foundations of your business — Core Values, Core Focus, Targets, Marketing and your Leadership Team."* **Operations:** `⚙️ Operations` + sub *"Your weekly cadence — track the numbers, run team meetings, ship quarterly goals, surface and resolve issues."* CSS rules added inline (same as planning.html) since each page maintains its own `<style>` block.
- **v0.5.174** — Moved the three primary actions to the home page. User: "Move one page plan to home page and remove it from planning and strategy and also move run weekly meetings and view one page operations to home and remove it from operations." Added a new `.quick-actions` block at the top of both `standardMode` and `parentMode` on `business.html` containing: **🗓️ Run Weekly Meeting**, **📋 View One-Page Plan**, **📋 View One-Page Operations**. New `wireRunMeetingButton(btnId, activeId)` JS helper (parametrised so it works for both the standard-mode button id `runWeeklyMeetingBtn` and the parent-mode button id `runWeeklyMeetingBtnParent`) — same create-or-open-this-week logic as the old `operations.html` button. Removed the old "Go to This Week's Meeting" `meeting-btn` that sat between Core Values and This Week panels. The Next-Meeting stat tile and the This Week panel's "View Meeting ›" link still navigate to the actual meeting row when one exists. **Removed from `planning.html`** and **`strategy.html`** the `<a class="view-plan-btn" href="one-page-plan.html">` block — these hubs now focus on activity cards only. **Removed from `operations.html`** both the Run Weekly Meeting button and the View One-Page Operations button, plus the `wireRunMeetingButton()` function and its call (~38 lines of JS gone). Operations hub now focuses on the 4 activity cards (Weekly Numbers / Goals / Meeting / Issues).
- **v0.5.173** — Hotfix: the green pill was missing from the home page (`business.html`). User: "There is no green pill heading on Home Screen." Cause: when I stripped the in-body BUSINESS row in v0.5.172, the `<span id="activeBizName" class="biz-pill">` element that `js/active-org.js` needs to inject and relocate had also been removed earlier (back in v0.5.164 when the navy header was cleaned up to drop the `← Account` back link). Without that span in the navy header, `relocatePillToSubToolbar()` finds nothing to move into the sub-toolbar, and the pill silently doesn't render. Fix: re-added the empty `<span id="activeBizName" class="biz-pill"></span>` to `business.html`'s `.header-left` (same position it occupies on every other business-level page). `active-org.js` now finds it on boot, populates it with `🏢 IASHQ`, relocates it into the sub-toolbar, and (since the IAS HQ account has 2+ businesses) upgrades it to a `<select>` for switching. The green pill now appears on the home page exactly as on planning / strategy / operations / etc.
- **v0.5.172** — Removed the in-body `BUSINESS: [▾]` row from the home page (`business.html`), restoring the compact green pill from `js/active-org.js` so the home page matches the rest of the business-level pages. User: "It's the name of the account. I'm not sure how to explain this." + "Show business: I want the green pill size the same as the other pages." The v0.5.163 in-body dropdown was a wider full-row affordance with a `BUSINESS:` label + a stretched `<select>`; on planning / strategy / operations / etc. there was already a small teal pill (from `active-org.js`'s sub-toolbar) — the home page looked different. Removed the `.biz-switcher` HTML block, the `.biz-switcher` / `.switcher-label` / `.switcher-select` CSS, the `.biz-switcher-bar { display: none }` rule, and the JS that built/sorted/wired the `<select>`. Now `js/active-org.js`'s sub-toolbar pill shows on the home page too — same size, same teal, same place as everywhere else. The parent / child context line (`↑ Part of …` / `↓ N children`) stays in its `.biz-extra-action` wrapper below the active-org pill. Trade-off: the parent-child group scoping logic added in v0.5.166/168 was specific to the in-body dropdown — now the active-org pill shows every biz in the active subscription. For this user that's still just the 4 IAS businesses (since they're all in the IAS HQ account); cross-account leakage isn't an issue here.
- **v0.5.171** — Restyled the top-right Account button on business-level pages from a white-translucent square chip into a **teal pill** with `🏛️ Account` label. User: "It's the square look instead of the green pill on top right." The `.header-account-btn` CSS in `css/style.css` switches from `background: rgba(255,255,255,0.15)` + `border-radius: var(--radius-sm)` (small square chip) to `background: var(--accent); border-radius: 99px;` — same teal as Open buttons / role pills / value pills elsewhere in the app. The HTML on all 22 business-level pages now renders `<span>🏛️</span><span>Account</span>` so the button shows the label, not just the icon glyph. Hover state darkens to `var(--accent-dark)`. Sign Out keeps its existing white-translucent chip style so the two buttons are visually distinct.
- **v0.5.170** — Reordered the three sections on `learning-vault.html` to match the user's mental rhythm: **Planning → Strategy → Operations**. User: "In learn Change the order planning, then strategy then operations." Previous order (set at vault creation in v0.5.119) was Strategy → Operations → Planning. Python script lifts the three `<div class="vault-section">` blocks out of the markup and writes them back in the new order; everything else (cards, links, descriptions, CSS) is untouched.
- **v0.5.169** — Removed the redundant `← Home` back-link from the navy header on the four hub pages. User: "Can you see this home name looks different. I am ok with home going back to the green pills." The hub pages (`planning.html`, `strategy.html`, `operations.html`, `learning-vault.html`) had a small white-translucent `← Home` link in the top-left of the navy header — same destination (`business.html`) as the green-pill 🏠 Home tab restored in v0.5.167, but visually inconsistent. Now the only "Home" affordance everywhere is the green-pill bottom-nav tab; the navy header on hub pages just shows "Your Business Coach". Deeper sub-page back links (`← Strategy` on core-values / core-focus / etc., `← Operations` on goals / issues / scorecard / etc.) stay — they go to a different destination than Home and are still useful one-tap navigation.
- **v0.5.168** — Sort the `BUSINESS: [▾]` dropdown so the parent always appears at the top. User: "In the click down it should be showing the hq at the top as this is the parent one and should be in order." Cause: in v0.5.166 the group was correctly filtered (parent + children only) but never sorted, so the raw membership order leaked through. Locale-aware alphabetical ordering put "IAS General / IAS Life / IAS Outsourcing" before "IASHQ" — counter to the visual hierarchy users expect. Fix: compute a `groupParentId` (which is `activeOrg.parent_organisation_id` when the active biz is a child, or `activeOrg.id` when it IS the parent), then sort `groupMems` with that id first and the rest alphabetically. Now on IASHQ the dropdown reads **IASHQ → IAS General → IAS Life → IAS Outsourcing**; switching to IAS General shows the same order (because the group's parent is still IASHQ).
- **v0.5.167** — `🏠 Home` tab restored to the bottom nav on every business-level page. User: "I think the dashboard for each group needs a home tab. It was removed but I think it needs to go back." Home was dropped in v0.5.147 because it overlapped semantically with `🏛️ Account` — but now Account lives in the top-right icon (v0.5.165) so there's no conflict anymore. With Home back, the user has a one-tap path from any sub-page (Goals, Issues, Core Values, etc.) to the business overview (`business.html`). Bottom nav on business-level pages is now 5 items: **Home / Planning / Strategy / Operations / Learn**. Home gets `.active` on `business.html` itself; on the other 21 business-level pages Home is regular (each page keeps its own active tab — Strategy active on strategy.html, Operations active on goals.html / issues.html, etc.). Patched via Python regex — insert before the existing first nav item on all 22 files. Account-level pages (index.html / account-users.html / account-setup.html) unchanged.
- **v0.5.166** — Bugfix: the `BUSINESS: [▾]` switcher on the parent / child dashboards was listing every business across every account the user owns. User: "The iashq parent page has the wrong business connection as it is including all of the businesses. Should only include the parent and child pages for ias." Cause: in `business.html` the dropdown was populated from the raw `memberships` array (which intentionally spans subscriptions for the active-org chrome) without filtering. Fix: compute a `groupMems` array based on the active biz's parent/child relationship: if the active biz has a `parent_organisation_id`, include the parent and all siblings (children sharing that parent); otherwise (active biz is itself a parent or a standalone), include self + any direct children. Both the option rendering and the change-handler now use `groupMems`. Result: on IASHQ the dropdown shows IASHQ + IAS General + IAS Life + IAS Outsourcing — never any SARUBA biz; on IAS General it shows the same 4 (parent + 3 siblings). Standalone businesses still appear alone in the dropdown. **Child cards section + financial rollup were already correctly filtered** to the parent's children only (via `childrenInThisAccount` in v0.5.155) — only the switcher was leaking cross-account.
- **v0.5.165** — Account control moves off the bottom nav on every business-level page → up to a 🏛️ icon button in the top-right of the navy header, next to Sign Out. User: "I just think the flow of working on the account is not as good when it is clicked on. Need an intentional reason to go to the account area." Bottom nav on business pages drops from 5 to 4 items (Planning / Strategy / Operations / Learn) so working in a business feels focused; Account becomes a distinct top-right chrome element you tap intentionally to exit the flow. New CSS rule `.header-account-btn` in `css/style.css` (white-translucent rounded square, 32×28, `margin-left: auto` to push self + Sign Out to the right of the flex header). 22 business-level pages patched via Python regex — both the button insert before `#signOutBtn` AND the Account item removal from the bottom nav, in one pass. Account-level pages (index.html / account-users.html / account-setup.html) unchanged — they already had no Account button and use their own 3-tab bottom nav.
- **v0.5.164** — Removed redundant back/up nav from the business dashboard. User: "I think the parent and child pages don't need back arrows and links at the top as they can use the tabs." Dropped the `← Account` link from the navy header on `business.html` and the `Account dashboard ›` link from the `.biz-extra-action` row — the bottom-nav 🏛️ Account tab already gets you back. The bizContext row (`↑ Part of …` / `↓ N child businesses`) is now wrapped in a `#bizContextRow` that's hidden by default and only shown when `renderBizContext` finds parent/child info to display — so on a standalone biz there's no empty action row taking up space. Net effect on the parent/child dashboards: header is just "Your Business Coach" + Sign Out; the in-body `BUSINESS: [▾]` switcher from v0.5.163 is the primary identifier; bottom nav covers navigation. Other business-level pages (strategy/operations/etc.) still have their own `← Home` / `← Strategy` chain in the navy header — that's a separate cleanup if you want.
- **v0.5.163** — Business dashboard now uses a `BUSINESS: [▾]` switcher row mirroring the `ACCOUNT: [▾]` row on the account tabs. User: "Yes to 1 [BUSINESS: switcher row]. I just like consistency." Replaced the `.greeting-row` block (big bold biz name + date + switch link) on `business.html` with a `.biz-switcher` row using the same CSS rules as `.acct-switcher` on the account tabs (border-bottom, padding 16/14, dropdown with `flex:1; max-width:100%`). The dropdown is populated with every business the user has access to in the active account; change handler sets the active org via `window.activeOrg.set` and reloads. A small `.biz-extra-action` row below holds the `↑ Part of <parent>` / `↓ N child businesses` context (left) and an `Account dashboard ›` link (right). Also adds `.biz-switcher-bar { display: none !important; }` so the v0.5.141 sub-toolbar pill from `js/active-org.js` doesn't double up with the new in-body switcher. Removed the now-orphaned `todayDate` JS write. Net effect: when you tap into IASHQ (parent) or IAS General (child), the top of the page reads `BUSINESS: [IASHQ ▾]` in exactly the same style as `ACCOUNT: [Insurance Advisory… ▾]` on the account tabs.
- **v0.5.162** — Child cards on the parent dashboard now visually match the biz cards on the account dashboard. User: "The sections in parent and child fix the look of the name of business to match the account." Aligned `.child-card` styling in `business.html` parent-mode to `.biz-card` styling in `index.html`: border-radius 10 → 12, padding 12×14 → 14×16, name font-size 0.95rem → 1.05rem, head gap 8 → 10. Added mobile media query (≤ 600px) mirroring the account dashboard's: child-card-name takes full row with `flex-basis: 100%` and shrinks to 1rem so the pills/buttons wrap nicely on phones. Card hover state was already similar; tightened the transition to match (`box-shadow .15s`). `position: relative` added so future indent markers (↳) can attach. Net effect: scrolling between the IASHQ dashboard's "Child Businesses" panel and the account dashboard's biz list now feels visually continuous.
- **v0.5.161** — Account dropdown now stretches identically across all three account-level tabs. User: "This version still is different on first page" (after v0.5.160). Root cause: `.switcher-select` on `index.html` had only `min-width: 180px`, no `flex: 1`. On Users + Setup the rule was `min-width: 180px; flex: 1; max-width: 100%;` so the dropdown stretched to fill the row. Result: Businesses dropdown rendered at 180px with empty space to its right; Users/Setup dropdown stretched full-width. Added the missing `flex: 1; max-width: 100%;` to index.html's rule. The `Account: [dropdown ▾]` row now renders pixel-identically across all three tabs.
- **v0.5.160** — Account switcher row now identical across all three account-level tabs. User: "Account : is different on first page to the other 2." The `+ New client account` button was inline with the switcher on `index.html`, which (a) made the dropdown narrower than on Users/Setup and (b) made the switcher row visibly taller. Moved the button to its own row (`.acct-extra-action`) right below the switcher, right-aligned. Removed `margin-left: auto` from `.switcher-btn` since it no longer needs to push itself to the right of a flex row. Net effect: the `Account: [dropdown ▾]` line looks pixel-identical on Businesses / Users / Setup, with the create-account button still discoverable as a separate row on Businesses only.
- **v0.5.159** — Audit + cleanup of the three account-level tabs. User: "Audit the account three tabs for consistency and ensure that all of the links work." Found 4 things and fixed them: **(1) Bug** — `index.html` was missing the `membership_status = 'active'` gate that `account-users.html` and `account-setup.html` have, so a user whose membership lapsed could still see Businesses but get bounced from the other two. Added the same gate. **(2) Consistency** — `index.html` had no `.page-header` block while Users + Setup did (titles "👥 User Management" / "⚙️ Account Setup"). Added a matching "💼 Business Management" page-header to index.html (the section title under it drops the briefcase emoji to avoid doubling). Page-header CSS rules (.page-header / .page-title / .page-sub) added to index.html's `<style>`. **(3) Cleanup** — `<span id="activeBizName" class="biz-pill">` removed from the navy header on `account-users.html` and `account-setup.html`. The span was hidden via the `.biz-switcher-bar { display:none }` rule but shouldn't be in the markup on account-level pages. **(4) Resume pill** — `js/active-session.js` (the floating "Resume Planning Session" pill) was loaded only on `index.html`. Added to `account-users.html` + `account-setup.html` so the pill follows you across all account tabs. Audit also confirmed: bottom-nav links resolve correctly with right `.active` classes on each tab; all 100+ `getElementById` references across the three pages match elements in markup (scripted check); sign-out wireup present on all three; footers now uniformly read v0.5.159. The `+ New client account` button stays Businesses-only on purpose — each tab has its own primary creation action.
- **v0.5.158** — Consistent chrome across the three account-level tabs. User: "The business setup needs to be consistent I think over the 3 pages." `account-users.html` and `account-setup.html` now have the same `Account: [dropdown ▾]` switcher at the top that `index.html` has — populated from every subscription the user owns, change-handler sets the active sub + clears the active-org cache + reloads (same behaviour as Businesses). All three tabs also `display: none !important` the `.biz-switcher-bar` sub-toolbar that `js/active-org.js` injects — a business pill is meaningless at the account level. Footers updated everywhere (account-users.html was stuck on v0.5.153, account-setup.html same). Version-bump checklist in this file expanded from 5 to 7 files so the two account-level pages get bumped too. **No SQL.**
- **v0.5.157** — Block deleting a parent business while it still has children. User: "I think the parent needs to be set up not to deleted unless all children are removed." Two-layer guard: **(1) SQL** — `delete_business` RPC now counts rows in `organisations` where `parent_organisation_id = business_id` and raises `Cannot delete this business while it is the parent of N child business(es). Unlink or delete the children first.` if any exist. Defence in depth — even if the UI is bypassed (curl, dev console, future client), the RPC refuses. **(2) UI** — on `index.html`, the `⋮ → Delete` option on a row that has children is rendered with `disabled` + `title="Has children — unlink them first"` and the label changes to `Delete (has children)`. CSS adds a muted italic style for `.actions-menu button:disabled`. To delete a parent, the admin sets each child's Parent dropdown to `— None —` first (one tap per child via the inline picker from v0.5.149/150), then Delete becomes available. **Requires SQL** — see `supabase/v0.5.157-delta.sql`.
- **v0.5.156** — Removed the duplicate account name on the Businesses tab. User: "The name of the business is showing twice on account page." `index.html` had both an `<select>` switcher (showing the active account name + letting you pick between accounts) AND a big `<h1>` below it showing the same account name (e.g., "🏢 Insurance Advisory Service NSW Pty Ltd"). Dropped the `<h1>` block + the `renderAccountHeader()` function + the call in `refresh()`. The switcher dropdown alone now serves as the account-name indicator. Side effect: the page feels less top-heavy, and the bold "💼 Your Businesses" section title becomes the primary heading. `account-users.html` and `account-setup.html` unchanged — they don't have the duplication.
- **v0.5.155** — Parent business dashboard becomes a holding-co rollup view. User: "The parents dashboard needs to be more focused on a summary of entire businesses. I don't think the current parent is as helpful." When `business.html` loads and the active biz has at least one child (any other org in memberships with `parent_organisation_id === activeOrg.id`), the standard own-business dashboard (Stat tiles / 1-Year Goal / Core Values / Meeting button / This Week / This Quarter) is replaced with four new panels: **Group Strategy** (parent's 10-Year Vision / 3-Year Outlook / 12-Month Goal), **This Quarter's Priorities** (parent's rocks for the current quarter), **Group Financials** (1-Year Revenue Target summed across, plus last-12-months Revenue / Expenses / Profit summed across `financial_periods`), and **Child Businesses** (tappable card per child with open issues + goals-on-track + next meeting). Tapping a child card switches the active biz to that child and reloads, landing the user inside that business's standard dashboard. Period filter: 12 most-recent `YYYY-MM-01` strings, exclusive of current month. Standalone and child businesses are unchanged — they get the existing own-business dashboard.
- **v0.5.154** — Moved the "Business Management — add, rename, delete businesses…" copy on `index.html` out of the account header (where it sat under the bold account name) and down to a `.section-blurb` line right below the "💼 Your Businesses" section title. User feedback after v0.5.153: "The wording for business management should be under the heading of businesses." With long account names (e.g. "Insurance Advisory Service NSW Pty Ltd" wrapping to two lines on phones), having a sub-paragraph between the account name and the section header made the top of the page feel cluttered. Now the account header is just `🏢 [Account Name]`, and the descriptive copy belongs to the section it describes. `.section-blurb` is small grey text (0.78rem, line-height 1.5) with a negative top margin so it tucks snugly under the section title. `account-users.html` and `account-setup.html` unchanged — their page-headers don't have a competing section-title below them.
- **v0.5.153** — Account dashboard split into three tabbed pages. User request: "The accounts area needs to have 3 different areas. Business management, user management and setup of of accounts name (invoicing information)" + "Tabs at the bottom like the other pages are structured." Replaced the single 🏛️ Account tab on the account-level bottom-nav with three tabs: **💼 Businesses** (`index.html`), **👥 Users** (`account-users.html` — new), **⚙️ Setup** (`account-setup.html` — new). `index.html` keeps the business-list + inline parent-picker UI but loses the Your Team section, all team modals, and the rename-account modal. `account-users.html` is self-contained: same team-card UI, Edit-team-member modal, Invite, Remove. `account-setup.html` has an auto-save form for **Account name + Billing email + Billing address + Tax ID (ABN / VAT)** — uses the new `subscriptions.billing_*` columns from v0.5.152. Each page loads its own subscription + memberships independently. The header context line on `index.html` now reads "💼 Business Management" so the page's purpose is clear. **No new SQL** — the billing columns and the team_members RLS hotfix shipped in v0.5.152.
- **v0.5.152** — SQL-only release. **Hotfix for the Edit team member save** — user reported "Could not save: permission denied for table users" when saving via the v0.5.151 modal. Root cause: the long-standing RLS policy `"invited user accepts own invite" ON team_members` used `(SELECT email FROM auth.users WHERE id = auth.uid())` in its `USING` clause. The `authenticated` role doesn't have direct SELECT on `auth.users` in current Supabase, so any UPDATE that caused that policy to be evaluated would fail with "permission denied for table users". Single-row UPDATEs apparently dodged it for reasons unclear; the multi-row `.in('id', memberIds)` UPDATE that v0.5.151's save handler does triggered it. **Fix:** drop and recreate the policy using `auth.jwt() ->> 'email'` (a JWT claim) instead of querying the table — same semantics, no `auth.users` read. **Also bundled in the same migration:** new `billing_email`, `billing_address`, `billing_tax_id` columns on `subscriptions` for the upcoming 3-tab Account Setup page (v0.5.153). See `supabase/v0.5.152-delta.sql`.
- **v0.5.151** — User cards on the account dashboard are now clickable, opening an **Edit team member** modal. User: "I can't click into it. There needs more options the user as they can be admin some or all." The modal shows: avatar + name + email, a Display name input, and a per-business role picker for every business the current user is admin of. Picker options: `— No access —`, `Member`, `Coach`, `Admin`. Save handler: changes the role on existing team_member rows via direct UPDATE, removes access via the existing `remove_team_member` RPC (which guards against removing the last admin), and adds new access via `invite_team_member` RPC when an email is available. Caveat: existing-and-signed-up users have their `invited_email` cleared on signup (per the `link_pending_invites` trigger), so we can change/remove access for them but not add them to a *new* business from this modal — the picker shows "Member/Coach/Admin" as disabled with a hint "No email on file — use Invite User to add". Pending-invite users and the current user themselves keep full add capability. Follow-up: a small RPC to look up emails for an admin's team would close that gap.
- **v0.5.150** — Two fixes on the account dashboard. **(1) Mobile layout** — biz names like "IAS General" and "IAS Outsourcing" were wrapping mid-word because the head row was crammed with name + Parent pill + admin pill + reorder + Open + ⋮. Added a `@media (max-width: 600px)` rule that forces `.biz-name` to `flex-basis: 100%` so the name always gets its own line, with the pills + actions wrapping to a second row. Pills slightly smaller (0.58rem) and child indent slightly tighter (22px) so the inner content gets more room. **(2) "Add child" picker** — user feedback: "There needs to be a clearer option to pick a child I think." Previous v0.5.149 design only had a "Parent" picker on each row, which required you to stand on the child saying "my parent is X" — counter-intuitive when you're thinking top-down. Now every standalone admin row also shows a `Add child: [Pick a business… ▾]` picker that lists every other standalone admin biz; picking one sets that biz's `parent_organisation_id` to the current row in one tap, then re-renders. Rows that ARE parents show `Children: IAS General, IAS Life, IAS Outsourcing` (read-only chip-list) instead of a Parent picker — same depth-1 lock as before, just clearer wording.
- **v0.5.149** — Account dashboard becomes a compact admin table. User feedback: "I don't understand why it can't just be more of an admin dashboard where I can reallocate in a table … I don't need the revenue and open issues etc in the account area." Removed: the 4 stat tiles at the top (Businesses count / Users count / Open Issues / Goals On Track), the snapshot grid inside each biz card (Revenue, Quarter Goals, Open Issues, Next Meeting), the `renderStats()` function, the `snapshotByOrg` map, and four parallel data-fetch queries (`targets`, `rocks`, `issues`, `meetings`) that only existed to feed those — the dashboard now loads noticeably faster. Each biz row now has an inline **Parent** dropdown right under the name + role + open/actions row. Change it → auto-saves → re-renders the tree so other rows' candidate lists refresh. Depth-1 enforced by only offering top-level rows as candidates; a biz that already has children shows *"Has children — can't have a parent"* in italics instead of a picker. Removed the v0.5.148 "Structure" modal + button (now redundant). Tree indent on children kept (teal left border + ↳ marker + 28px margin) so visual hierarchy stays. `business.html` unchanged.
- **v0.5.148** — Moved parent/child allocation from the per-business dashboard up to the account dashboard. User feedback: "It needs to be easier and done in accounts area. I think allocation is accounts level." The previous v0.5.145 design put the picker inside each business's `business.html`, which (a) made you tap into 4 separate businesses to wire up an IAS-style tree and (b) made it easy to set the direction wrong (the picker is for THIS biz's parent, not its children — confusing when you're on the parent). New design: on `index.html`, a small `🏛️ Structure` button appears next to `+ New Business` in the businesses header (admins of ≥2 businesses only). Tapping opens a modal listing every admin-controlled business in the account, each with a "Parent" dropdown next to it. You can wire up the entire tree in one place. Save commits all changed parents in parallel, busts the header switcher cache, reloads. Depth-1 enforced client-side (can't pick a parent that itself has a parent). Removed the "Business Structure" panel from `business.html` and the `wireBusinessStructure` JS — the read-only `↑ Part of IASHQ` / `↓ N children` context line under the greeting stays as the "where am I in the tree" indicator. No SQL change.
- **v0.5.147** — Removed the `🏠 Home` item from the bottom nav on every business-level page. User: "I think now home and account are the same?" — diagnosis confirmed: `🏠 Home` (links to `business.html`) and `🏛️ Account` (links to `index.html`) felt semantically overlapping. Since "home" really means "go to the top-level dashboard" in most users' mental model, and that's what `🏛️ Account` already does, `Home` was redundant. Bottom nav on business-level pages is now 5 items: **Planning / Strategy / Operations / Learn / Account**. The navy-header back-link chain (`← Home` on hubs, `← Strategy`/`← Operations` on sub-pages) is unchanged, so users can still reach `business.html` in 1 tap from a hub and 2 taps from a worksheet/tool — same paths as before, just no shortcut tab. 22 files patched via Python regex.
- **v0.5.146** — Stripped the bottom nav at the account level down to a single **🏛️ Account** item. User feedback: "the IAS dashboard should have no reference to strategy and operations or Learn. They should all happen at the next level down." The previous 6-item nav on `index.html` was misleading — Strategy / Operations / Learn (and Home / Planning) all linked to business-level pages that assume an active business is selected, which is meaningless at the account dashboard. Applied to four files: `index.html`, `account-strategy.html`, `account-operations.html`, `account-planning.html`. Business-level pages keep the full 6-item nav (no change). Side effect: `account-strategy.html` / `account-operations.html` / `account-planning.html` are now reachable only via legacy links from `annual-sessions.html` / `quarterly-sessions.html` / `team-checkins.html` — to be revisited (delete vs re-link from a body button on `index.html`) in a follow-up.
- **v0.5.145** — **Phase 1 of parent/child business relationships.** Adds a nullable `parent_organisation_id` on `organisations` so a business can be linked to a parent (e.g., IASHQ → IAS General / IAS Life / IAS Outsourcing). Flat tree only — depth 1 (the UI prevents picking a business that itself has a parent). **Account dashboard (`index.html`)** now renders businesses as a tree: parents at top with a "Parent" pill, children indented underneath with a `↳` prefix and a teal left border, standalones afterwards. Reorder arrows only show on top-level cards. **Business dashboard (`business.html`)** gets a new admin-only "Business Structure" panel — picker for the parent (top-level siblings only), chip-list of any current children. The greeting row also shows a header context line: `↑ Part of IASHQ` for children, `↓ N child businesses` for parents. **Core Values (`core-values.html`)** is the first inheritable section: a child with no own row of values reads from the parent and shows a teal banner *"Inherited from IASHQ — [Override locally]"*. Click "Override locally" → child's row is created from a snapshot of the parent's values; banner becomes amber *"Overridden locally — [Revert to parent]"*. Revert deletes the child's row and re-inherits. Inputs are disabled in inherited mode so the user can't accidentally type into a parent's data. **Requires SQL** — see `supabase/v0.5.145-delta.sql`: adds the column + self-parent CHECK + index + three additive RLS policies so children can `SELECT` parent's `core_values` / `core_focus` / `targets` (Phase 2 + 3 won't need another migration).
- **v0.5.144** — Added the Account nav item to `index.html` (marked `.active` since it IS the account dashboard) and to the three account-level hubs (`account-strategy.html`, `account-operations.html`, `account-planning.html`). User feedback after v0.5.142: "But if this is the case where is the account area located. Because it's not down bottom." Diagnosis: the v0.5.142 change skipped `index.html` and the account hubs on the (mistaken) theory that "a back-to-here button is redundant" — but that made the bottom nav inconsistent (5 items on account-level pages, 6 on business-level pages), which made the change invisible from the page the user was actually viewing. Fixed by adding the 6th item everywhere; on `index.html` it gets `.active` to indicate "you are here". Nav is now uniformly 6 items across every page.
- **v0.5.143** — Fixed the version label on the account dashboard. `index.html`'s footer was stuck on `v0.5.89` because the version-bump checklist only covered `business.html`'s footer — `index.html` was missed for ~54 versions. User report: "I only see v 0.5.89" on the account dashboard, even though business pages and the live `VERSION` file showed v0.5.142. Bumped index.html's footer to v0.5.143 and added `index.html` to the five-files version-bump checklist in CLAUDE.md so this doesn't drift again. The actual functionality (Account nav item added v0.5.142, etc.) was already live — only the visible label was misleading.
- **v0.5.142** — Added an **Account** item to the bottom nav, sitting after Learn (🏛️ icon → `index.html`). User feedback: "the flow needs to be easier to get back to the main accounts page. can we add this as a box next to learn?" Previously the only way back to the multi-business account dashboard was through the navy header's `← Back` (which goes to `business.html`, not `index.html`) or the breadcrumb chain. Now every business-level page (22 files including `business.html`, all 4 hubs, every operations tool, every strategy worksheet, planning list pages, sessions, learning vault) has a one-tap path to the account dashboard. The bottom-nav is now 6 items: Home / Planning / Strategy / Operations / Learn / Account. Account-level pages (`account-*.html`) and `index.html` itself are unchanged.
- **v0.5.141** — Header layout rework. User feedback: "move the pill for the drop down out of the main header and put it to the right side of the screen and I want it a square 'pill'. And Your business coach more at the top." Done — `js/active-org.js` now detaches `#activeBizName` from the navy header on init and moves it into a new sticky sub-toolbar (`.biz-switcher-bar`) right under the header, right-aligned. Pill border-radius dropped from 14px → 6px (square-ish). Removed the v0.5.124/130 hide-title-on-phones CSS rule since the pill no longer crowds the navy bar — "Your Business Coach" now shows on every viewport and is slightly bolder (font-weight 800, 1.05rem on phones).
- **v0.5.140** — Manual reordering for businesses. New `sort_order` column on `organisations` (default 0). On the account dashboard, each biz card gets a small ↑/↓ pair (admins only, hidden when there's just one biz). Click swaps sort_orders with the adjacent biz, persists both, re-renders. The v0.5.139 navy-header switcher dropdown picks up the new order too (cache busted automatically). **Requires SQL** — see `supabase/v0.5.140-delta.sql`.
- **v0.5.139** — Business-switcher dropdown in the navy header. User feedback: "if I'm in issues I need go to each issue for each business with a drop down." When the active subscription has 2+ businesses, the `🏢 [Biz Name]` pill on every business-level page is now a `<select>` listing every biz the user is admin/coach/member of in the active account. Picking a different biz calls `activeOrg.set(...)` and reloads the same page — so you stay on Issues / Goals / Scorecard / Meeting / etc. but viewing the other business's data. Lazy-loads Supabase only when needed; caches the business list in localStorage for 5 minutes. Account-scope session pages still show `🏛️ [Account]` (v0.5.135) — the override now marks the pill `data-overridden="1"` so the switcher doesn't replace it back.
- **v0.5.138** — "Make a goal →" button on the issues modal. User asked for a way to promote an issue into a quarterly goal "on the back of" the issue — common flow in IDS: discuss issue → decide it's a strategic priority → make it a goal. Button appears on existing issues (not on the Add-new modal, since the issue needs an ID to be promoted). Click confirms, then: (1) inserts a new `rocks` row for the current quarter with the issue's description + owner pre-filled, (2) marks the issue as `resolved` with the solution note "Promoted to Q[n] [YYYY] goal: '[description]'", (3) navigates to `goals.html` so the user can finish setting up the new goal (set SMART criteria, on/off-track status, etc.). Uses the active org's `currentQuarter()`.
- **v0.5.137** — Added a Financial Outlook strip to the Operations one-pager (`one-page-operations.html`) and the account-level operations carousel (`account-ops-plans.html`). Two compact columns at the bottom: **Last Year** (full-year totals) and **This Year** (YTD totals), each showing Revenue / Expenses / Profit. Reads from the same `financial_periods` table the Strategy one-pager uses; aggregates by calendar year (Jan–Dec). The carousel version buckets per business so each card shows that business's own annual totals. Empty business years show "—". Print rules added so the strip stays on the same landscape-A4 page as the rest of the doc.
- **v0.5.136** — Inside an account-scoped annual or quarterly session, the area-link buttons now point at the account-level carousel pages instead of the business-scoped worksheets. User report: "the links in the annual session ie targets needs to go to the IAS area, not the outsourcing." Diagnosis: links like `Open Targets →` were hardcoded to `targets.html`, which is biz-scoped — so it opened whatever business was last active (Outsourcing, in this case), not the account context. **Fix:** added an `ACCOUNT_LINK_MAP` in `run-annual-session.html` + `run-quarterly-session.html` that swaps `targets.html → account-targets.html`, `core-values.html → account-values.html`, `core-focus.html → account-focus.html`, `marketing-strategy.html → account-marketing.html`, `leadership-team.html → account-leadership.html`, `goals.html → account-goals.html` when the session row has `subscription_id` set (no `organisation_id`). Biz-scoped sessions are unchanged.
- **v0.5.135** — Fixed a UX bug on account-scoped planning. User reported: "iasHQ - click on planning - start a planning day and it's opening the general business." Diagnosis: the SQL is correct (sessions are inserted under the right subscription_id), but the navy header pill `🏢 [Biz Name]` was still showing whatever business was last active (Coach4U from SARUBA, in their case) — making an account-scoped session look like it belonged to that biz. **Fix:** on `annual-sessions.html?scope=account`, `quarterly-sessions.html?scope=account`, `team-checkins.html?scope=account`, and their workspaces (`run-annual-session.html`, `run-quarterly-session.html` when the loaded session has `subscription_id` set), the pill is now overridden to `🏛️ [Account Name]` so the user can see at a glance that they're in account-level mode. Also hid the "View One-Page Plan" / "Open Quarterly Goals" hint links on the account-scope list pages — they link to business-scoped destinations that don't apply at account level.
- **v0.5.134** — Better error toasts on the three "+ New" buttons (Annual / Quarterly / Team Check-in) so a swallowed insert error now surfaces actionable text. If the column `subscription_id` is missing → "SQL not applied — run supabase/v0.5.132-delta.sql". If RLS denies → "Permission denied — you need to be the account owner". If a CHECK constraint fails → "Schema mismatch — re-run v0.5.132-delta.sql". Otherwise the raw Postgres message. Helps the user self-diagnose instead of guessing why the button "isn't working".
- **v0.5.133** — Hygiene fix: `supabase/schema.sql` lines 59-60 now use `DROP FUNCTION IF EXISTS … CASCADE` instead of bare `DROP FUNCTION IF EXISTS`. When schema.sql is run on a populated DB (which shouldn't happen — schema.sql is for fresh installs; deltas are for incremental migrations), the bare DROP fails because the `financial_periods` policy (added v0.5.117) depends on `user_org_ids`. CASCADE makes the DROP succeed and cleans up dependent policies — schema.sql goes on to recreate them. No app-level change.
- **v0.5.132** — **Dual-mode planning + new Team Check-ins module.** Planning sessions (annual + quarterly) and a new check-in batches concept can now live at either business level (existing) or account level (new). User can pick when creating. The new `team-checkins.html` + `run-team-checkin.html` pair lets admins run pulse surveys outside any planning session — name + date + invitees + share link + aggregated results. Batch results are **admin-only** (RLS). Account-level batches let an admin analyse pulse data across every business in the account. Existing per-session check-ins (annual/quarterly) unchanged. Also added: **delete-meeting** button on `meeting.html` so an unintentional meeting can be removed. **Requires SQL** — see `supabase/v0.5.132-delta.sql`: new `checkin_batches` table, both `annual_sessions` and `quarterly_sessions` get a nullable `subscription_id` (and their `organisation_id` becomes nullable; a scope CHECK enforces exactly one set), `team_checkins.session_type` widened to include `'batch'`, two new helper functions `user_account_ids` and `user_admin_account_ids`, and RLS rewritten across the three session tables to handle both scopes.
- **v0.5.131** — Improved the Annual Planning Session workspace. User feedback: "the EOS way is too structured" — want richer, flexible capture. Added three new sections to `run-annual-session.html`: **(1) Session Resources** — a list of `{label, url}` external links so the user can paste their own Zoom recording, slides, Google Drive folder, photos URL etc. No files are uploaded; the app only stores text URLs. **(2) Session Notes** — a big free-form textarea for decisions, energy of the room, what surprised you, what you'd do differently next year. **(3) Personal Commitments** — a list of `{name, commitment}` for the "One Thing" each leader commits to for the year (carries into quarterly sessions). All three auto-save (500ms debounce) and mobile-friendly. **Requires SQL** — see `supabase/v0.5.131-delta.sql` which adds `notes text`, `external_links jsonb`, `commitments jsonb` columns to `annual_sessions` (no RLS changes — existing policies cover them). Keeps the existing 4-area checklist as a lightweight preflight; the new sections are where the substance lives.
- **v0.5.130** — Three things: **(1)** Dropped all dead Coach4U sample-data seeds from `business.html`'s `ensureSeeds()` (the strategy ones were already gone in v0.5.113; this version removes the demo operations + planning seeds — `coach4u_demo_meetings`, `coach4u_demo_rocks`, `coach4u_demo_scorecard`, `coach4u_demo_issues`, `coach4u_annual_sessions`, `coach4u_quarterly_sessions`). 130 lines of dead code gone; every page now reads from Supabase. The function stays as a no-op so the existing callsite in `init()` doesn't have to be edited. **(2)** Rewrote `operations.html`'s "Run Weekly Meeting" button to use Supabase (was the last reader of `coach4u_demo_meetings`) — finds or creates a `meetings` row for this Monday and navigates to `run-meeting.html?id=…`. **(3)** Mobile keyboard handling: new `js/mobile-keyboard.js` wired into all 61 pages — listens for input focus, waits ~350ms for the keyboard, and scrolls the focused field to the centre of the viewport so iOS doesn't hide it. Pair with `scroll-margin-bottom: 260px` on inputs in `css/style.css`, and `body.keyboard-open .ws-save-bar { transform: translateY(120%) }` to slide the save-bar out of the way while typing. **(4)** Restored the "Your Business Coach" title in the navy header on phones for account-level pages — the v0.5.124 fix was too aggressive and left `index.html` with an empty left side on first launch. New rule uses `:has(.biz-pill:not(:empty))` so the title only hides when there's a populated biz pill competing for space (business-level pages).
- **v0.5.129** — VB2 source arrived (`EOS-VB2-Implementer-Guide.pdf`, moved into `/EOS/`), so the two guides the audit couldn't fully verify against a source got the missing depth. **Marketing Strategy** rewritten from ~200 words to ~700: DGP framework (Demographic / Geographic / Psychographic) → "The List" as a filter; 3 Differentiators competitor-line-up test ("you're the only one that does all three" — Southwest 3LFs reference); Process and Guarantee as explicit YES/NO decisions; Rule of 7 callout. **Targets** 3-Year and 1-Year sections got the "shot over the bow then debate" framing, the specific bullet prompts (employees, clients, locations, products, role), the 5–15-bullet target with "err on leaving something on", and the full **see-it / want-it / believe-it** three-question test (not just "see it"). 1-Year Plan got the explicit reference to Issues List + Org Chart when answering "what are the most important things", one-at-a-time onto-the-board, and the full closing read-back. Also moved the uploaded `EOS-VB2-Implementer-Guide.pdf` from the repo root into `/EOS/` to match the rest.
- **v0.5.128** — Fixed a wording bug in the Annual Planning Session guide's Coach's tip. Reported by user: the tip in the Annual guide said "if you wait, it'll slip into a busy quarter, then slip again" — recycled phrasing from the Quarterly tip that doesn't fit the annual cadence (annuals slip across years, not quarters). Rewrote to: "If you wait, the date gets eaten by busy quarters and the year quietly disappears." Front-end one-line copy fix.
- **v0.5.127** — Removed the standalone Values Discovery interactive exercise. User feedback: "I don't think the run the values guide is needed. It's just not helpful compared to the comprehensive other document." Deleted `learn/values-discovery.html` entirely. Removed the "Run the Values Discovery exercise" link from `learning-vault.html`'s Core Values card and from the bottom CTA row of `learn/core-values.html`. The deepened Core Values guide (v0.5.125 — 800 words covering the 100-of-them prompt, two-pass Keep/Kill/Combine, Lencioni's three traps, the People Test + Five-Use Test) replaces it; running the exercise from the guide is the new flow.
- **v0.5.126** — Deepened the other 9 light Learning Vault guides to match the depth of v0.5.125's Core Values rewrite. Each now has a structured exercise section with timings, the actual prompts the facilitator runs, the keep/kill/combine and silent-write-then-share patterns, and the closing visualisation / read-back checks that the EOS implementer guides use. Sources: the EOS implementer guides under `/EOS/` (Focus Day, VB1, Quarterly Rocks, Scorecard, Level 10, Issues Solving Track, Quarterly Debrief), plus the Lencioni 5-Dysfunctions framework and Covey's Big Rocks metaphor (both non-EOS public references). No EOS trademark terminology in user-facing copy — the v0.5.116 wording rules still apply. The Marketing Strategy, Financials, and Team Check-in guides were left as-is (audit verdict: SOLID / no EOS source to compare against).
- **v0.5.125** — Rewrote `learn/core-values.html` with full EOS-implementation depth. User noted the guide was "a little light on" compared to the original EOS implementation experience. The new guide adds: the **"100 of them" prompt** (think of 3 people you'd want 100 of), the **2-hour exercise flow** (silent listing → characteristics on the board → two passes of Keep/Kill/Combine separated by a break), **Lencioni's three value traps** (Accidental / Aspirational / Permission-to-play, with a test for each), and **two tests for finished values** (the People Test — score 3–5 real team members 1–5 against each value; the Five-Use Test — would you actually use this to hire / fire / review / reward / recognise?). Grew from ~200 words to ~800. No EOS trademark terminology used (rules from v0.5.116 preserved) — the methodology comes from the EOS implementer guides in `/EOS/`, the Lencioni traps from a 2002 HBR article. Parity audit of the other 12 guides is running and will inform v0.5.126.
- **v0.5.124** — Decluttered the navy site-header on phones. At ≤ 640px viewports the bar was packing four competing elements (← back, "Your Business Coach" title, 🏢 biz pill, Sign Out) into ~390px and reading as busy. Hid the title text on phones — the brand presence is preserved everywhere else (PWA name on the home screen icon, page H1 below the header, back link to the parent section). Bar now reads `← Back · 🏢 [Biz Name] · Sign Out`. Slightly tightened padding (12 → 10px vertical) and gave the biz pill more room (max-width 110 → 160 at 480px, 90 → 120 at 400px). No change above 640px — tablet/desktop header is unchanged.
- **v0.5.123** — Fixed the broken PWA install. Three things were wrong: (1) `manifest.json` had a stale `/external-Coach4u-app/` `start_url` + `scope` + icon paths from a previous repo, so iOS launched the installed app at the old URL and couldn't load the icon (fell back to its auto-generated "C"). Rewrote with relative paths (`./`, `./icon-192.png`, `./icon-512.png`). (2) Only `business.html` and `setup.html` declared `<link rel="apple-touch-icon">` (and both pointed at `favicon.svg`, which iOS doesn't reliably honour as a home-screen icon). Added `<link rel="apple-touch-icon" sizes="180x180" href="apple-touch-icon.png">` to every page (57 files, including learn/). (3) Generated real PNG icons (180/192/512) showing **BC** — white B + teal C — on the navy gradient background, matching the brand. Also updated `favicon.svg` to match the new BC mark.
- **v0.5.122** — Smart back link on every Learning Vault guide. Before: the guide's header back link always went to the Vault index, even if the user had just clicked through from a worksheet (e.g. arrived at `learn/quarterly-goals.html` from `goals.html`'s tip box). After: each guide now checks `document.referrer` on load — if the visitor came from the matching worksheet/tool, the header back link swaps from `← Learning Vault` to `← Quarterly Goals` (or whichever section it was), so "back" actually goes back. If they came from the Vault index, the link stays `← Learning Vault`. The explicit `← Back to Learning Vault` link at the bottom of every guide stays put — the Vault is always one click away regardless.
- **v0.5.121** — Deep-linked the in-page Learning Vault tip box on every worksheet and operations tool to its specific guide. Before: clicking the teal "Explore X in the Learning Vault →" inside `goals.html` (or any other worksheet) dumped you on the generic Vault index, where you had to find the matching card. After: it goes straight to `learn/quarterly-goals.html` (the activity-specific guide), labelled "Read the Quarterly Goals guide →". Applied to all 10 activity pages (5 strategy worksheets + Financials + 4 operations tools). The bottom-nav "Learn" tab still goes to the Vault index — that's the entry point for browsing all activities.
- **v0.5.120** — Wrote actual content for every Learning Vault guide. v0.5.119 shipped the index but the "Read guide" buttons were placeholders; the user pushed back ("I don't see what value you have offered, the links don't go to the actual page"). This version creates **13 real guide pages** under `learn/<slug>.html` — one for each activity — each with What this is / Why it matters / How to do it well / Common pitfalls / Coach's tip. The Vault now has a live `Read the guide →` primary CTA on every card. The worksheet link drops to a secondary ghost button on each card. The Core Values card keeps the Values Discovery exercise as a third action.
- **v0.5.119** — Rebuilt the Learning Vault as a structured activity index mapped to the full Coach4U program. 13 cards across 3 sections: **Strategy** (6 — Core Values, Core Focus, Targets, Financials, Marketing Strategy, Leadership Team), **Operations** (4 — Quarterly Goals, Weekly Numbers, Weekly Team Meeting, Issues), **Planning** (3 — Quarterly Planning Session, Annual Planning Session, Team Check-in). Every card has an **Open worksheet/tool** primary CTA (live) and a **Read guide · Coming soon** ghost button reserving the slot for future written content. The existing `learn/values-discovery.html` exercise stays — surfaced as the secondary CTA on the Core Values card. No SQL, no schema.
- **v0.5.118** — Simplified the Financial Outlook on `one-page-plan.html` and the `account-plans.html` carousel. Previously the strip rendered a 12-row × 4-column mini-table per side (month × Rev × Exp × Profit + totals row); now each side shows just the **12-month totals** as three big lines: Revenue, Expenses, Profit. Less visual noise on the printed one-pager. The `financials.html` worksheet is unchanged — that's still where the per-month figures live.
- **v0.5.117** — **Monthly financials on the one-page plan.** New `financials.html` worksheet (linked from Strategy) lets the admin/coach enter monthly revenue + expenses for the **last 12 months (actuals)** and **next 12 months (forecast)** — 24 month rows total, profit auto-calculates per row + totals. Auto-saves to a new `financial_periods` Supabase table (`(organisation_id, period)` unique, nullable revenue + expenses). RLS: members read, admins+coaches write — same pattern as the rest of the strategy tables. The **bottom strip of `one-page-plan.html`** now renders a two-column financial outlook (Last 12 / Next 12 side by side, month × Rev × Exp × Profit, with totals row). **`account-plans.html`** carousel renders the same block per business so cross-business reviews include the financials. Multi-tenant aware — every read is `eq('organisation_id', orgId)`; the carousel scopes to the active subscription as of v0.5.115. **Requires SQL** — see `supabase/v0.5.117-delta.sql`.
- **v0.5.116** — Two big sweeps. **(1) Multi-tenant scoping** applied to the 12 remaining account-level carousel pages (`account-numbers`, `account-goals`, `account-meetings`, `account-issues`, `account-values`, `account-focus`, `account-targets`, `account-marketing`, `account-leadership`, `account-annual`, `account-quarterly`, `account-checkins`). Each now loads `js/active-org.js` and filters the `team_members` query by `organisations.subscription_id = activeSub.id`, matching the fix already applied to `account-plans.html` and `account-ops-plans.html`. **(2) EOS de-jargoning** — removed every verbatim EOS term from user-facing copy: "Level 10 Meetings" → "Weekly Team Meetings"; meeting agenda "Customer & Employee Headlines" → "Customer & Team Highlights"; "Issues — Identify, Discuss, Solve" → "Issues — Discuss & Resolve"; "Our three uniques" → "What makes us different"; "Our Proven Process" → "Our Process"; "3-Year Picture" → "3-Year Outlook" across worksheets and one-pagers; "quarterly Rocks" → "quarterly priorities". Rewrote all 17 team-checkin questions (replicated in `team-checkin.html` / `run-annual-session.html` / `run-quarterly-session.html`) to drop "Accountability Chart", "right seat / right people", "Quarterly Projects", "Annual Meetings", "10-Year Target", "3-Year Target" — order and meaning preserved so historical 1-5 scores still map to the same conceptual statements.
- **v0.5.115** — Account-level companion to v0.5.114. New **`account-ops-plans.html`** — carousel of every business's one-page operations doc, linked from `account-operations.html`. Same scoping fix backported to `account-plans.html` (strategy carousel).
- **v0.5.114** — New **`one-page-operations.html`**, parallel to `one-page-plan.html`. Landscape A4, three columns: Quarterly Goals · Weekly Numbers · Open Issues. Linked from `operations.html`.

## Current Status
- **Account dashboard** (`index.html`) — post-login landing; multi-tenant switcher + account header + stats + businesses list + users list (all Supabase-backed).
- **Business dashboard** (`business.html`) — single-business workspace; reads 1-Year Goal + Core Values panels from Supabase (`targets` + `core_values`), scoped to active organisation. Empty-state placeholders when no rows.
- **Hubs** — `strategy.html`, `operations.html`, `planning.html`, `learning-vault.html` — uniform Design 1 layout. Each links to a printable one-pager (`one-page-plan.html` for strategy, `one-page-operations.html` for ops; planning has no one-pager yet).
- **Strategy worksheets** — Supabase-backed, scoped by active org. Feed both `one-page-plan.html` and `account-plans.html`.
- **Operations tools** — Supabase-backed (rocks, scorecard_metrics + entries, issues, meetings + headlines + todos). Feed `one-page-operations.html` and `account-ops-plans.html`.
- **Planning sessions** — Supabase-backed (`annual_sessions`, `quarterly_sessions`). Workspaces use attendance checklist + team check-in share link. Aggregated check-in results visible to the whole team.
- **Team Check-in** — `team-checkin.html` is **auth-gated** (login required; honours `?returnTo=` param). 17 rated statements + name + optional role. Submissions write to `team_checkins` keyed by session_id + session_type. EOS-flavoured wording removed in v0.5.116.
- **Resume pill** — floating "Resume Planning Session" pill on every page when a session is in progress (`coach4u_active_planning_session`).
- **Mobile**: Responsive at 390px and 768px breakpoints.
- **Login**: Gold standard v2.2.
- **Accountability Chart & Team Alignment**: MOVED to `yourteamcoach`.

## Architecture (Supabase + Multi-Tenant)

The app is fully on Supabase. Team-scoped, role-based, multi-tenant.

### Team model
- A **buyer owns a `subscription`** with N seats and is the first Admin of every business inside it. One buyer may own multiple subscriptions (one per client account).
- Each team member has their own auth login but data is **team-scoped, not user-scoped** — one shared dataset per organisation.
- **3 roles**:
  - **Admin** — full edit access; invites/removes other members; renames/deletes the business itself.
  - **Coach** — admin-equivalent for data + team operations (full edit, invite/remove), but **exempt from per-seat billing**. Cannot rename or delete the business. Use case: the coach lives inside the client's account without consuming one of the client's seats.
  - **Member** — reads team data; submits check-ins; cannot edit team data.

### Schema overview (see `supabase/schema.sql` for the source of truth)
```
subscriptions   (id, owner_user_id, name, seat_count=3, included_businesses=1, status, stripe_*)
organisations   (id, subscription_id, name)
team_members    (id, organisation_id, user_id, invited_email, role, status, display_name, joined_at)
                — role: 'admin' | 'coach' | 'member'  |  status: 'pending' | 'active' | 'removed'
users           (id, email, membership_status)   — mirror of auth.users for gating

# strategy + operations + planning data
core_values, core_focus, targets, marketing_strategy, leadership_team_members
scorecard_metrics, scorecard_entries, rocks, issues
meetings, meeting_headlines, meeting_todos
annual_sessions, quarterly_sessions, team_checkins
                — all scoped by organisation_id
```

### RLS pattern
- **READ** (most tables): active team member of the org.
- **WRITE** (most tables): admins + coaches of the org (helper `user_admin_org_ids()` returns both roles).
- **`team_checkins`**: read by any active member; INSERT by the submitting member only.

## Pricing Model

**Source of truth for pricing and payment links: https://cathcoach4u.github.io/coach4uapp-dashboard/admin.html**

All Stripe payment links and pricing are managed in the admin page above — not here and not in the app code. Update pricing or links there. The notes below are for development context only.

- **Per business, not per user** — users are unlimited on every plan. Billing unit is the business/entity.
- **A business or entity** = any organisation in the account (holding company, subsidiary, standalone — each counts as one).
- **Self-serve up to 5 businesses** — 6+ requires a contact us conversation.
- **AI add-on** — all AI features are gated behind an optional monthly add-on. Base software works fully without it. Price TBD at AI launch.
- **Services** — onboarding, coaching retainer, data migration, training, integrations. Quoted per engagement, not automated through the platform.
- **Coaches are billing-exempt** — full admin-equivalent access, not counted in billing.

## Add a New Member (SQL)

```sql
INSERT INTO users (id, email, membership_status)
SELECT id, email, 'active'
FROM auth.users
WHERE LOWER(email) = LOWER('email@here.com');
```
